# Codex Top 开发约定

- 这是独立的原生 macOS 应用，SwiftUI 负责内容，AppKit 负责窗口。最低 macOS 14。
- 发布默认使用正式版本号，不再自行添加 Beta 标识；历史版本和截图来源记录保留。
- 需求、设计与阶段验收入口是 `docs/README.md`。代码变更同步更新对应文档。
- 每次接续先读 `docs/STATUS.md`，每阶段结束更新决定、验证结果、遗留问题和下一步。外接屏是正式验收场景。
- 刘海、展开面板、浮窗共用一份关注列表；图钉只改变窗口模式。常驻浮窗与刘海互斥，钉住只留浮窗，取消回刘海，系统状态栏入口保留。
- Codex 数据只读。禁止修改 Codex 数据库、配置、任务记录或认证文件。禁止记录对话正文、密钥和完整本机路径到公开诊断材料。
- 缺失、过期和无法判断的状态必须明确展示，不能伪装成运行或完成。
- 缺失运行开始时间先保留 `--:--`，只允许从明确本轮开始记录回查并经兼容校验补时间，不用任务创建时间、mtime 或当前时间估算；后台回查不能改写当前任务状态。
- 测试使用合成样例和独立临时目录；演示模式不读取真实任务。应用验收必须包含打包后的真实窗口。
- 桌面调试只使用一个 Computer Use 会话，由主代理统一操作；子代理只做代码、文档或只读审查，不并行控制桌面。复用同一调试应用，测试后清理残留鼠标提示，避免叠出多个虚拟鼠标。
- 用户不接受两份监控界面并存。调试不得让真实版与示例副本同时可见；优先在真实实例上验证，测试后确认只剩预期实例，不能让示例刘海与真实圆环同时占据桌面。
- 用户正在使用真实包时，不把 Demo 临时切到其桌面；本轮已因此造成示例任务无法跳转的混淆。合成状态验收先使用独立测试环境；需要向用户展示演示包时提前说明，结束后退出并移回隐藏测试目录，恢复真实包和原偏好。
- 执行 `swift test`、`swift build -c release` 和阶段对应的实际验收后再提交。用户已要求本次工作阶段性 commit/push；只提交本项目变更，禁止强制推送。
- 不把本机 `.codex`、应用状态、任务标题缓存、构建产物或原始诊断输出提交到 Git。
