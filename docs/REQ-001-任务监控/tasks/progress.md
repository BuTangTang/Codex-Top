# 阶段进度

| 阶段 | 交付 | 状态 | 提交 / 验收证据 |
|---|---|---|---|
| M1 | 数据接入、任务状态、关注规则、测试 | 已提交并推送 | `76576e7`；[验收](../../validation/M1.md) |
| M2 | 刘海、浮窗、圆环、状态栏、选择器、设置 | D-52 已实现；92 项测试与列明主题窗口核对完成 | [beta.3](../../validation/beta3.md)，完整鼠标/物理屏/FPS 边界保留 |
| M3 | 打包、真实数据验收、CI、使用说明 | beta.3 通用 DMG、桌面更新、双语说明和实拍完成，已公开预发布 | [beta.3](../../validation/beta3.md)，CI 仍缺 workflow scope |

更新：2026-09-11。当前 D-52 主题扩散轮廓已修复，中文/英文 README 和完整交接提示词同步；构建、实际窗口和镜像检查见 [beta.3](../../validation/beta3.md)。

历史 D-49–D-51：紧凑字号、13 档比例、淡橙标签与待处理固定时长已实现。92 项测试、通用构建和包校验通过，实际截图已补；完整验收范围见 [beta.2](../../validation/beta2.md)。Windows 提示词已同步，Windows 软件尚未实施。

beta.1 已完成提交/推送及公开预发布，其 78 项测试与产物只对应 D-48；当时锁屏导致截图和完整拖动未验，现保留为历史记录。更早 17:06/17:14 的方向、运行行与已结束区证据不替代 beta.2 字号或比例验证。

以下按历史检查点保留证据。旧包的测试数、校验值、偏好和现场结果不作为本轮最终状态。未运行的动作、未覆盖的条件或未发布的版本不能记为通过；验收仍按 R01–R19 跟进。

D-28 检查点当时停止桌面操作，只完成代码检查、构建和文件交付，最终包未启动实测；以下后续阶段逐项更新状态。逐帧/FPS、系统减少动态效果与物理屏幕等缺项保持未验。

随后按用户要求退出残留的 Computer Use 提示服务，未继续应用验收；桌面调试改为单一会话，由主代理统一执行，具体约定见 AGENTS.md。

D-29 浅色背景模糊修订随后完成双架构构建与独立桌面更新，最新产物见 M2-circle 文末。未为此再次启动 Computer Use 或应用实测，背景文字的模糊仍需确认。

[逐项验收对照与外部条件](../../validation/acceptance-matrix.md) 是当前收尾入口。

## D-30 / D-31 真实数据交付

- [x] 当时实现账户额度客户端、60 秒刷新、超时/取消/冷却与日志回退；新增 8 项合成测试及一次生产客户端真实读取。日志回退后被 D-43 移除，当前主额度只用账户接口。
- [x] 任务 UUID 链接、精确定位 Codex 应用、官方用量网页入口；新增 2 项路由测试。
- [x] 状态栏完整尺寸弹层、正确坐标与比例、关闭不留黑块；与顶部 hover 逻辑分离。
- [x] 最终 48 项测试、actual universal release、严格签名、ZIP 完整性、桌面副本一致性通过。
- [x] 已退出并备份旧 Demo，启动桌面真实应用；保留用户浅色/80%/菜单栏与实际任务选择。
- [x] 本次调试提示服务已退出。用户此前确认当前玻璃“不闪、正常”。
- [x] 真实任务/额度前景后来由 D-32 实际包 AX 与截图补充确认；共享 showTasks 路径展示 80% 完整面板，点击任务/额度后收起。最初窗口获取超时仍保留为当时记录，不替代实际图标点击和其余比例验收。
- [ ] 真实对话与官方用量网页终点、物理屏幕及完整动画缺项仍按验收对照跟进。

本段更新此前“只交付 Demo、未启动”的历史状态。最终产物及证据范围见 [额度与跳转记录](../../validation/usage-navigation.md)；旧条目仍只表示当时检查点。

## D-32 任务点击崩溃修复

D-30/D-31 已在 `2899e83` 提交并推送。收尾时发现真实点击任务触发 LaunchServices 后台回调的 MainActor 断言，随即修复外层 completion 隔离；带 actor 检测的后台队列实验旧写法 SIGTRAP、新写法成功。重新执行 48 项测试与 actual universal 打包通过，桌面副本已更新。

真实包复测：任务/额度前景可见；点击真实任务面板正常收起且进程保持；恢复仅状态栏/浅色/80%，共享 showTasks 快捷键展示完整面板，点击额度后窗口隐藏。图标实际左/右键、90%/100%、Codex 与网页终点仍未证明；本段补充之前未验项，具体产物及证据见 [D-32](../../validation/usage-navigation.md)。

## D-33–D-37 刘海与圆环修订

- [x] D-33：状态栏弹层每次布局读取当前按钮坐标，屏幕变化、找回及按钮暂不可用时正确重新定位/回退；新增 2 项 resolver 测试。
- [x] D-34：显示名称改为“刘海模式”且兼容旧偏好；浅色状态条和展开面板跟随主题，物理摄像头区域仍黑。左侧待处理优先，右侧最多两行真实剩余额度；新增 8 项摘要测试，覆盖缺失、实际周期和过期边界。
- [x] D-35：原生运行弧连续旋转，定期任务刷新不重新起转；圆环开合等待真实动画 completion 后再收整窗口，失效代次不能执行旧收尾。
- [x] D-36：固定 44pt 内增加 11pt 的 `!` 与淡琥珀/失败红色提醒底，运行数保留；展开或处理完撤除，减少动态效果显示静态提醒。用户认可状态设计图并要求继续优化。
- [x] D-34 初版真实 80% 刘海模式：浅色两翼、左运行/右周额度与展开内容可见；用户反馈方黑块、硬白边和灰色，后续 D-37 修改不沿用此截图作为最终轮廓验收。
- [x] D-35/D-36 最终编译代码的隔离 Demo 端点：44pt 琥珀底、内部 `!`、中心运行数 2 可见，点击展开三条示例；不代表全部状态或 FPS。
- [x] D-37 当时实现摄像头底角 9pt、外底角 12pt、浅色收起表面白色 30%、边线 opacity 0.14、摘要 13pt regular/5pt 状态点；双翼每侧 98→88pt、几何样例宽度 376→356。两态材质后续有 D-39 候选调整，D-40 仅讨论；本条只记历史方案。
- [x] 最终 58 项测试再次通过：核心 24、几何 14、状态 2、跳转 2、额度 8、摘要 8；universal、签名、ZIP 完整性、桌面二进制一致性通过。
- [x] 最终真实应用已更新并启动（Demo=false），真实任务及日志额度可见；点击真实任务后收回、应用保持运行，未核对 Codex 对话终点。
- [x] 用户要求解决示例模式混淆：临时 Demo 已退出并恢复其测试偏好，Demo app/ZIP/SHA 移至隐藏 `.local/qa-apps`。保留真实应用原关注列表、圆环/浅色/80%；以后不在用户日常桌面切换 Demo，本轮不再改模式。
- [ ] D-37 修改后的最终刘海轮廓尚未再次实测；真实待处理各状态、减少动态效果、连续开合和全部中间帧仍需按实际场景验证。
- [ ] 物理显示器重新排列/主屏切换、拔插/合盖、减少动态效果与完整动画时间线/FPS。两项 resolver 测试只验证坐标解析，不能代替硬件操作。

D-37 交付与证据统一记录在 [刘海与圆环修订](../../validation/notch-orb-refinement.md)。该阶段 ZIP SHA-256 为 `116e373440a2ad9587bb600faf2ac007231dff60de16b647f731afd9f677f0a7`，二进制 SHA-256 为 `1250905822b9417f6e45378767acee818f5705922eaa3e8372a50c249f634d84`；不能作为后续新包标识。当时两个提示服务已正常退出并核对，此结论不扩展到后续调试。本段不预先声明提交或推送。

当时另记退出风险：取消额度请求后立即终止应用，没有等待拥有的 CLI 清理完毕；这是代码路径发现，未运行复现。D-43 后续已补有界异步收尾，不能继续把“未改此路径”列为当前状态；真实应用极端退出时序仍待验。

## D-38 拖动松手误放大

根因是 endDrag 松手主动恢复 hovered，触发 1.08 倍悬停反馈。现改为真实拖放后等待指针先离开，保留正常重新进入的反馈；首次手势位移和松手总位移共同参与 4pt 点击阈值判定。58 项测试与 universal、签名和 ZIP 通过，真实桌面包已更新启动；已看到真实任务及账户额度，但没有重新操作用户的顶部拖动来冒称实机通过。用户随后自行改为刘海模式/浅色/80%，后续保留其当前选择，不能仍写固定使用圆环。

## D-39 / D-40 同宽与菜单栏背景衔接

- [x] D-39：`topPanelFrames` 使 `.top`/`.floating` 的顶部面板两态共用 x、宽度和顶部；共宽 `max(410 × scale, cameraWidth + 176)`，按可见宽度一起裁限。菜单栏弹层与圆环不受影响。
- [x] 新增 3 项几何测试，覆盖各比例、负坐标、侧边 Dock 与摄像头安全区；候选总计 61 项测试通过：核心 24、几何 17、状态 2、跳转 2、额度 8、摘要 8；universal 构建通过。
- [x] 额度摘要采用主文字色、14pt medium，修正此前 13pt 辅助灰色的低对比度。D-39 把两态统一为额外白色 24%、0.5pt/opacity 0.20 描边，后随 D-42 真实包交付。
- [ ] D-40 用户要求先讨论背景衔接；透明底、文字色随系统变化只是建议，未确认、未实施、未交付。用户指向的是右侧系统菜单栏底色，先前按摄像头解释有误，不能将该误判写成确认需求。
- [ ] D-40 透明方案未实施，暂时保持已有外观；误删的软件黑遮罩已恢复 9pt 底角。刘海/浅色/80% 仅为当时偏好；保留用户后续选择，不启动 Demo。D-39 的候选测试不构成透明方案实施证据。

D-38 对应的最后一个提示服务曾正常退出，之后另见服务进程；本轮最终清理结果待主代理记录，不能提前声明全系统无 Computer Use。最终产物与验收以 [本轮记录](../../validation/notch-orb-refinement.md) 的最新结果为准。

## D-41 / D-42 开头抖动、任务运行圈与文字

- [x] 定位任务行运行圈缺少动画：`ActivityIndicator.running` 原先固定 -70°；现复用 NativeRunningArc，持续 1.2 秒一圈，保留 22/24pt、3pt 轨道。减少动态效果静止、不可见停转，44pt 圆球默认视觉不变。
- [x] 调整圆环开合布局：backing canvas 坐标换算与 NSWindow resize 纳入同一 NSAnimationContext 事务，先 layout 后 defer display；expandedSize 内容直接落最终尺寸，外壳负责 morph，内层额外缩放 0.72→0.96。
- [x] 可读性代码：窗口宽度不变，缩放后标题最低 15pt、任务名 14pt、说明/状态/额度 12pt；辅助文字深色灰值 0.65→0.76、浅色 0.40→0.34。
- [x] D-42 阶段 61 项测试及 universal 构建通过，真实包已交付。该检查点结果不能代替之后 D-43/D-44 的统一构建。
- [ ] 用户在交付后仍报告首次展开抖动，因此不能标为已修复。任务行旋转、80% 字体、连续动画、减少动态效果和物理屏幕按实际证据分别验收，不由编译或端点截图推断全部通过。

此阶段现场偏好为圆环/深色/80%，保留用户后续选择及原真实列表。D-40 透明菜单栏方案未实施，不以动画修订代替背景方案确认。

## D-43 当前账户额度与退出收尾

- [x] 主额度真实模式仅用 `accountQuota`；日志独立为 `historicalQuota`，设置折叠项标注“历史日志额度（不代表当前账号）”。失败清空当前账户余额，不用旧账号或日志回填。
- [x] 每次实际额度刷新重新使用独立 CLI；自动约 60 秒、手动最短 5 秒、15 秒超时，无任务或对话启动。“暂停任务刷新”只停任务扫描，独立额度继续。
- [x] 目录代次、请求 ID、停止状态阻止旧响应回写；切目录后保留仍在清理的请求句柄，退出先等待拥有的请求回收再结束应用。
- [x] 新增 2 项合成回归：A→B 的下一次自动读取获取新账户且不发 turn，A 后新账户读取失败不复用缓存；该阶段 63 项测试通过。生产只读额度请求两次成功，耗时 1.44/1.59 秒，不公开私人额度值。
- [x] 已纳入本轮最终 76 项测试及 universal、严格签名验证，真实设置已见账户来源与独立历史折叠区。
- [ ] 未替用户登录或实际 A→B 换号；真实换号、极端应用退出时序仍按记录跟进，自动刷新不代表瞬时换号检测。

## D-44 已展开面板与列表位置

- [x] 固定顶边代码及 3 项几何回归随首轮 66 项测试、universal 候选通过；该结果不覆盖之后的方向改动。
- [x] 候选 QA 发现自动滚动会把运行行卷走；已删除延迟 50ms 的首个完成任务 scrollTo，将插入 move(.top) 改为 opacity。
- [x] 后续修改与 D-45 纳入最终测试构建；已结束区增减保持靠球边缘，向下保持 top、底部回退向上保持 bottom，空间不足滚动，不重新选方向。
- [x] 17:06 上一真实候选中，已结束区展开和收回都保留当前运行行，滚动位置为 0；不借此证明全部边缘或逐帧动画。

## D-45 圆环从原位置展开

- [x] 用户已确认实施：点击球后球消失，面板从原位置向下展开，top=orb.maxY、标题靠近指针；左右 clamp 到屏幕内侧。仅下面不够且上面更多时向上，空间不足则滚动；同次开合锁方向，收起回原锚点。
- [x] 本轮源码调整为先准备最终布局/canvas，再同一 withAnimation 提交 expanded 与 surfaceFrame；首帧按方向顶/底对齐。正文只保留 uiScale 与淡入，删除额外 0.96→1 中心缩放，不在展开结束后才切对齐。
- [x] 新方向纳入最终 76 项测试和 universal，上一真实候选实际点击后指针位于标题附近，展开方向符合要求。
- [ ] 全部边缘实测仍待补证；截图不能证明 FPS 或完全零抖，物理屏幕边界保留。D-47 桌面交付另见下段。
- [x] 已同步 Windows 提示词、STATUS、usage、需求与详细设计。D-40 透明菜单栏继续保留未实施边界，不能与本次已确认方向混淆。

## D-46 运行行与提问提醒

- [x] 排除目标任务取消关注、手动排除及归档；确定旧UI强制滚动与增量超过64KB重置两处独立问题。
- [x] 64KB 分块、每轮 4MB 顺序追赶，不跳尾丢等待/turn ID；`isCaughtUp` 到本次采样 EOF 后才对外恢复归约状态，积压期间仅该任务显示未知/正在同步，不加全局条幅。
- [x] 5 项回归随最终测试通过：大输出保留异步等待及普通工具/额度不解除、旧轮终止不误结束当前轮、跨预算和半行、来源同步/追平、`observedSize` 识别同大小原位重写与缩短但仍超过 offset。
- [x] 上一真实候选的已结束开合前后运行行保留；提问/回复按真实结构化元数据与合成链验证，不制造假提问。
- [ ] 冷启动及真实文件重置仍仅尾读 64KB，可能缺少更早未答事件；超过预算要分轮追赶，期间不冒称实时。完整真实提问提醒链和最终包端点按实际证据分别验收。

## D-47 统一开合进度

- [x] 只读参考 Atoll 固定提交 `e46a16092f1edf185f515c1c843af1e5d680ee69`，详见 [参考项目](../../design/参考项目.md)，未安装运行或复制代码/素材。
- [x] 控制器统一 critical spring，response 开 0.32/关 0.28、damping=1、blend=0；response 不是固定持续时间。`OrbRevealOpacity` 以 AnimatableModifier 的同一进度经 smoothstep 驱动正文与圆环淡入淡出，无固定延迟、无额外正文 zoom，原生阴影未变。
- [x] 最终 76 项测试通过，测试日志完成于 17:11:58；arm64/x86_64 universal 与严格签名验证通过。
- [x] D-47 桌面包已替换；17:14 真实运行主任务与展开的两条已结束项可见，滚动位置为 0。不由端点截图推断完整帧时间线、FPS 或完全无抖动。

D-47 检查点 ZIP SHA-256：`6fa25cd0ad82a5877c67e1d90a7a0f756c7753336e149da2d3ae090241a7523d`；可执行文件 SHA-256：`e68b080fc854f86635bf1fbabf2e8aeb1c40c90ffb456b2f74bd1a6c311242dd`。仅用于识别该包，不能作为后续 D-48 最终产物。

## D-48 顶部直接拖动，不自动切状态栏

- [x] 用户明确取消全部拖放自动切状态栏：常驻浮窗、收起或展开圆环都不再触发吸附提示/自动模式切换；菜单手动选择“仅状态栏”保留。
- [x] 确认常驻浮窗与展开圆环的顶部整片区域可直接拖动，包含标题、空白和上下留白，按钮除外；无需长按，不再局限于左侧 16px 命中区。
- [x] 确认展开圆环拖动时同步平移球锚点，收起回到移动后的位置，按钮仍执行原有操作。
- [x] D-48 已实现并通过 78 项测试、universal 与严格签名，后发布 beta.1；产物见下面历史分享包记录。
- [ ] 完整实机顶部拖动当时因锁屏未验；用户现已解锁，随 beta.2 核对各顶部区域即时拖动、按钮不误拖、顶部释放不切状态栏、展开球移动后收起到新位置及手动菜单切模式。

## beta.1 交接与提交（历史）

- [x] 整理 [Windows 开发提示词](../../handoff/windows-implementation-prompts.md)：总提示词、6 个阶段、合成回归、源码映射与现场核验点；同步 D-46 预算/重写边界、D-47 弹簧参数及 D-48 顶部拖动/取消自动切模式，不预设框架、Windows 数据路径或深链能力。
- [x] beta.1 对应代码、Windows 提示词及分发记录已在 `bb5463a` 提交并推送，预发布回执见下。后续 beta.2 的测试、提交与发布须单独记录，不沿用此结果。


### D-48 构建与首次分享包

- 78 项测试、universal、严格签名通过，开发包已替换桌面并启动。
- v0.1.0-beta.1 DMG 已生成，挂载后核对真实模式、版本、Applications 链接、说明、签名和二进制一致，并已卸载。
- 用户当时要求 README 增加实际应用截图，曾采用隔离副本与明确标注的示例任务。beta.1 该次检查时 Mac 锁定，截图和实际拖动未完成；这是历史限制，用户现已解锁，不再以等解锁作为当前下一步。
- 发布标识和哈希见 [修订记录](../../validation/notch-orb-refinement.md)。

- 发布回执：`bb5463a` 已推送 main，`v0.1.0-beta.1` 公开 Pre-release 已发布，DMG/校验文件/Windows 提示词三个附件上传成功，服务器 DMG 摘要与本地一致。桌面真实应用当时同步 beta，截图副本和调试提示服务已清理；该清理结论只对应当时检查。

## D-49 / D-50 副屏字号、提醒对比与比例滑块

- [x] D-49 按每个窗口的实际 screen 提供字号环境：外接屏紧凑、内置屏常规，screen 暂不可得保持该窗口原档位；常驻浮窗始终紧凑，不用全局主屏代替各窗口。
- [x] 紧凑字号基准减 2pt，有效下限为 `max(11pt, 原下限 - 2pt)`；标题/任务/辅助基准 15/14/12pt，下限 13/12/11pt，常规下限保持 15/14/12pt。监控、选择器、额度和摘要一致，原生设置字号不改。
- [x] 浅色待处理最终统一橙色 `(0.91, 0.43, 0.06)`、12% 淡橙底与黑色 82% 字；深色亮琥珀配 tint opacity 0.12。文字底片圆角 4pt、水平/垂直留白 5/2pt，状态语义不变。
- [x] D-50 已实现 60%–120% 滑块，每 5% 一档共 13 档，当前百分比与恢复 100%；`MonitorScale` 统一有限值、范围和步长舍入，旧 80/90/100 偏好兼容，圆环保持 44pt。
- [x] 需求、详细设计、使用说明、Windows 提示词与 beta.2 发布草稿已同步；beta.2 新建预发布，不覆盖 beta.1 附件。
- [x] 新档位坐标浮点尾差采用 0.0001pt 容差，92 项全量测试与最终通用构建、签名和镜像校验通过。
- [x] 已补实际程序截图、13 档控件值、恢复 100%、小档头部/选择器/时长、浅深颜色和常驻窗口。
- [ ] 完整鼠标拖动、内置/外接所有组合仍待核对，不由端点截图替代。
- [x] beta.2 已提交、推送与独立预发布，回执见下一节；物理拔插/合盖、真实换号、跳转终点和 FPS 等边界仍按逐项验收记录。


## 2026-09-11 D-49–D-51 收口

92 项测试、universal、严格签名和 beta.2 DMG 挂载校验通过。原生 13 档比例、恢复 100%、小档布局、浅深截图和待处理固定时长完成列明范围核对。真实桌面应用已更新，截图副本和唯一提示服务退出；源码 `5d592b5` 已推送，注释标签指向同一提交，[beta.2](https://github.com/BuTangTang/Codex-Top/releases/tag/v0.1.0-beta.2) 已公开预发布，三个附件摘要核对一致。完整鼠标拖动、物理屏幕和 FPS 边界见 [beta.2](../../validation/beta2.md)。


## D-52 主题扩散和交接整理

- [x] 刘海上方直角、底部连续圆角与快照一致；其他表面按实际比例与曲线裁剪。
- [x] 92 项测试、通用构建、DMG只读挂载及桌面真实更新完成。
- [x] 内置刘海75%双向主题切换、常驻浮窗两态、实际示例截图与中英文README核对，Windows提示词同步。
- [x] 源码 `00bfd6f` 已提交并推送，独立 beta.3 预发布与三个附件摘要均已核对；见 [beta.3](../../validation/beta3.md)。
