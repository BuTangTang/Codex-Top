# Codex Top 独立部署包审查

2026-09-24。对应 REQ-002 M4；本页区分部署包合成检查、隔离环境真实运行和公网产品验收；云端基础服务已运行，手机业务尚未整体验收。首版24项结论保留在下方历史记录中；当前范围以本批双服务结果为准。

## 本批：双服务维护与独立复核

本批在同一 `codex-top` Compose 项目中管理 `server` 与 `https` 两个角色，继续使用 [deploy/codex-top](../../deploy/codex-top/README.md) 作为唯一配置与维护入口。业务仅绑定宿主回环端口，Caddy 仅公开 TCP443；代理只挂自己的 TLS 目录和只读配置。没有扩大到 PVTC 的服务、数据、网络或端口。

原部署任务完成38项合成检查，`python3 -B -m unittest -v test_manage` 全部通过。样例使用独立临时目录、真实 SQLite、tar 和文件替换，执行真实维护内部逻辑；Docker 命令、Compose JSON 输出及 age 子进程是替代边界。**这38项不证明真实容器启停、age 加密或公网 HTTPS 通过。** 本批代码身份为 `manage.py` SHA-256 `ef38e8978939ea7303e6332c4aa3444e8fb763d0d1c153b12a5344eced34bbcc`、`test_manage.py` SHA-256 `65c2eac4e15f2560fddebbd9b8b7cf087212ccf2caa144befc25394f9f861636`。

独立审查曾复现一项 P1：业务与代理同时升级时，业务启动前保存了尚未应用的新代理版本；业务启动失败后，实际仍为旧代理，正常业务回滚却因记录不匹配被拒绝。修复改为按角色推进发布记录：业务安装尝试保留当前已管理代理，代理真正更新后再记录其版本，仍保留真实漂移的拒绝规则。

独立复核重新执行原最小故障样例，结果通过：

- 组合升级中的业务启动故意失败后，原代理实例、镜像和运行状态不变，发布记录仍对应原代理。
- 使用升级前归档执行普通业务回滚成功，恢复业务运行；原代理和当前 TLS 保持不变。
- 该复核运行真实 `backup`、`start`、`load_release`、`rollback` 与临时 SQLite/tar，外部 Docker/age 仍为合成边界；不是生产恢复验收。

## 本批：主管已完成的隔离运行

以下按主管回报及当前 [部署说明](../../deploy/codex-top/README.md)、[状态记录](../STATUS.md) 归档，不扩展其验收边界。

| 检查 | 已确认结果 | 不包含的结论 |
| --- | --- | --- |
| 业务镜像 | `codex-top-server:2026.09.24-1`、Linux amd64；SQLite 迁移、UID1000、stdin 开户、停启后账号/密钥持久化，以及 HTTP 密码登录、解封、Bearer 身份回读通过；测试容器、卷和临时密码已清理 | 不代表经过本维护脚本恢复后的产品登录，也不代表远端已部署 |
| Compose | 本机真实双服务静态解析及维护配置校验通过 | 不代表38项合成测试调用了真实 Docker，也不代表目标主机配置已通过 |
| Caddy | 固定 amd64 Caddy 2.11.4，UID1000、drop ALL、仅 NET_BIND_SERVICE、no-new-privileges；仓库 Caddyfile 解析、正式 CMD 启动、本地管理健康和容器内443监听通过；临时容器已清理 | 测试无网络、无宿主公开端口，未证明公网 IP 证书签发、信任、续期或 WebSocket 连通 |

## 本批：Linux 真实 age 验收通过

独立任务已交回私有脚本 `linux-age-maintenance-check.py`，位于相邻源码仓被忽略的 `.local/req002-qa` 内，不随公开仓库发布。脚本仅需 Linux、Python 标准库及真实 `age`/`age-keygen`，默认90秒、最多180秒；密钥和样例均在独立临时目录，私钥权限600，结束时清理自身文件，不打印密钥内容。

脚本实际覆盖真实加密归档和解密、SQLite/上传文件/合成主密钥恢复、保留当前 TLS，以及密文尾部被破坏后不停止服务、不改当前业务或发布记录。维护 `backup`/`unpack`/`rollback` 和 age 子进程不替代；Docker、规范 Compose JSON、宿主资源/端口与 chown 是合成边界。因此，即使此脚本通过，也只补足真实加密与文件恢复验证，不能替代真实容器恢复、账号历史解密或手机业务验收。

主管已将固定维护源码与验收脚本传入独立 Ubuntu 24.04 构建虚拟机，使用系统 age 1.1.1 真实执行，exit0。加密/解密、SQLite/文件/合成主密钥恢复、当前 TLS 保留全部通过；尾部损坏的密文先输出超过64KiB明文再认证失败，维护流程仍在任何停服或数据变更前拒绝；归档/私钥权限600、临时目录清理均通过。Docker与宿主边界的合成范围保持，不据此声明真实容器或产品恢复通过。

本批仍需主管完成：目标主机基线和维护限额、真实维护启停与恢复、恢复后的账号/历史/文件读取、公网 IP TLS 签发及续期、WebSocket 与客户端重连、自有推送及小米送达，并对照 PVTC 前后业务状态。容器健康或归档检查不能单独作为 M4 完成依据。

## 本批：目标主机启动与公网 HTTPS

业务与Caddy归档均在目标机核对SHA-256后导入。经典Docker存储回读的实际业务config ID为 `sha256:0799193802b31ee52f02dd6fc8768df674ea18cd4ff14a87ddec7cb1825044da`，代理为 `sha256:2d8b1708bf8008935c0e2a9b6564f7f080cf9a73af3b27718f286230449b7101`；不把构建机多架构index ID误作远端config ID。

配置已安装在独立 `/opt/codex-top`。首次预检拒绝了宿主默认ACL继承导致的配置文件664；仅清除本项目新目录的继承ACL、显式设置目录700与配置600（Caddyfile644）后复检通过。系统Docker、Nginx、PVTC目录及ACL均未修改，安装age时禁止自动重启现有服务。

实际维护scope回读CPU `25000/100000`、内存256MiB、swap0、块设备读写各10MiB/s，验证通过后执行正式deploy。业务容器限制0.5CPU/1GiB、代理0.25CPU/128MiB，实际inspect一致。两角色health=healthy、重启数0；业务只发布回环18768，代理只发布TCP443。

- 服务器回环 `/ready` 返回200；电脑直连公网HTTPS `/ready` 返回200，curl未跳过证书验证。
- 独立Node TLS连接验证链受信任、IP SAN匹配、TLS1.3；颁发者Let's Encrypt YE2，证书有效期为2026-09-24 14:24:55至2026-10-01 06:24:54 UTC。真实自动续期尚未到窗口，未强制换证制造结果。
- 11个既有容器部署前后匿名状态/镜像/运行时间/重启/端口摘要完全相同，PVTC Web首页返回200；启动后MemAvailable约4.19GiB。该证据不等于完整PVTC工业业务回归，也不证明不存在瞬时资源争用。
- 主仓201项Swift测试及release构建通过；本批无Swift产品改动，未启动演示包或新增Mac窗口验收。

**剩余：** 云端账号登录与A/B隔离、认证WebSocket及断线恢复、手机→原Desktop同任务收发、公网通知/小米、恢复后的产品数据读取和证书续期。当前账号库未开户，不能把ready通过称为手机功能完成。

## 首版历史：交付范围

唯一正本为 [deploy/codex-top](../../deploy/codex-top/README.md)，共六文件：Compose、无秘密环境示例、忽略规则、管理脚本、对应检查与操作说明。复用既有服务器的轻量 SQLite 模式，没有另建业务服务。

- 固定 `/opt/codex-top`、独立 Compose 项目与网络、单服务和数据目录；宿主端口仅绑定回环，容器使用非 root 用户。
- 镜像必须是预装的固定摘要；CPU、内存、进程、日志及备份预算显式填写。配置示例没有未经核实的生产配额。
- 发布、备份与恢复只操作本项目服务。备份包含数据库、文件、主密钥和匹配的版本配置；恢复先完成解密认证及数据校验，再切换，保留切换前数据。
- 不操作 PVTC 服务、数据库、网络或卷；前后匿名状态比较只提供检查线索，不证明没有资源争用。

## 首版历史：审查与验证

主管复跑首版16项本地检查通过。独立审查发现两项 P2，已由原部署任务修复：

1. 容器停止后的检查失败不能绕过备份失败恢复；恢复前必须重新核对原实例身份与状态。
2. Compose 渲染的 CPU、内存/交换、PIDs 必须与归档所记录配置一致，不能通过改写 Compose 突破已审核预算。

修复先补失败复现，再收口为24项；主管最终运行 `python3 -m unittest -v test_manage.py`，24项通过、exit0。独立审查重新运行原故障样例，确认两项已解决：原实例停止后可恢复，身份变化/未知状态不启动；配置与渲染配额不一致会拒绝，等价小数与单位正常接受。部署包六文件已交回写权，README也已移除本批佳明验收。

这些检查使用真实 Compose 静态解析、临时合成 SQLite 和归档。Docker 状态及 age 外部调用使用替代实现，未连接 Docker daemon、未执行真实加密、未访问远端。

## 首版历史：当时的上线前剩余条件

- 独立 Linux 环境的正式镜像构建、容器启停、真实 age 加密恢复，以及恢复后的账号登录、历史解密和文件读取。
- 当前服务器资源与 PVTC 业务基线、空闲端口和有效维护限额，不能沿用历史数值。
- 独立 HTTPS 域名、证书、代理与长连接验证；自有 Expo/FCM 配置及小米实际送达。
- 原会话收发、审批、账号隔离、通知正确定位与 PVTC 前后业务对照。

未构建镜像、未部署、未修改 PVTC。佳明和原生新建仍按用户决定暂缓。
