# D-80 可选渐隐双弧

日期：2026-09-21。用户选择 D4“渐隐尾弧”，要求开始实施、保留原圆环并移除机器人。当前为 v1.0.0 build 36。build 34 的真实 44pt 效果被用户指出动效不够明显，build 35 据此增强弧宽、弧长、尾部亮度与转速；用户随后要求再加粗一点，build 36 仅将双弧 2.5pt 改为 3pt。

## 实现范围

- 设置提供“经典圆环／渐隐双弧”，共用原来的 44pt 圆形入口、任务列表、点击和拖动。原 StatusRing 与 NativeRunningArc 实现未改，WindowController 与上一已提交版本一致。
- 新样式为不透明黑白圆底、固定中心运行数和两段相隔 180° 的 80° 渐隐圆头弧，3pt 宽、3.2 秒匀速转动。浅色采用更深的蓝色；数字不转动、不缩放整球，运行数量不改变弧长。
- 待处理／失败优先，保留同时运行的数量和内部提示；全部完成、停止、未知、空闲具有独立符号。只在明确运行且可见时旋转，隐藏、展开、退出或减少动态效果时停止。
- 移除机器人视图和设置选项。旧 robot 值解码为 twinArc，未知未来外观字符串回退经典圆环，其他偏好不丢失；缺少字段的旧设置仍为经典圆环。

## build 36 验证

- 产品代码仅将双弧线宽 2.5pt 改为 3pt；半径自动调整为 18.3pt，仍保留 2.2pt 外边距。80°、3.2 秒、渐变、数字、经典圆环和窗口代码均未改。
- 173 项测试、release、arm64/x86_64 通用包、严格签名及 ZIP 完整性通过。黑白原生合成图已复核；几何和 99+ 的独立只读审查无阻塞问题。
- 已安装并运行唯一真实 build 36，外接屏实际 44pt 深色双弧、固定数字、点击展开和 Escape 收起通过。浅色实际窗口和经典切回沿用下方 build 35 的记录，本版不冒充重复验收。
- 安装与 dist 二进制 SHA-256 一致：`4452c861ef2fe097ccf95632dc0d55547846732e47cf58fbb4d5fcd070b6dcfd`。最新偏好按集合语义对比无变化，未显示 Demo，保留深色双弧供用户查看。

## build 35 验证

- 最终 `swift test`：173 项通过，无失败；包含 5 项外观／偏好兼容用例与 8 项原生双弧用例。验证旧 robot／未知字符串的整份偏好兼容、来源不可用、提醒优先和状态真实性，以及几何、固定数字、渐隐颜色、动画去重、隐藏／移除／减少动态效果与恢复相位。
- `swift build -c release`、arm64/x86_64 通用构建、Info.plist、严格签名及 ZIP 完整性通过。安装包与实际应用二进制 SHA-256 一致：`99151a86d46acdeec4694de7363796ecabbf40fc9a540d3081bdeaec099c0e35`。
- 合成渲染覆盖黑白主题、0／1／12／99+、等待／失败与运行并存、完成／停止／未知／空闲；1x/4x 导出和隐藏 NSWindow 的原生缓存绘制分别检查，未显示演示应用。颜色断言读取 sRGB 原始像素，避免 calibrated RGB 二次转换造成假失败。
- 已替换并启动真实 build 35，外接屏实际 44×44 像素窗口确认双弧、固定运行数与两次不同弧线相位；深浅配色、点击展开原任务面板、Escape 收起、切回经典圆环再切回双弧均通过。此为离散帧和交互端点检查，不是 FPS 测量。
- 安装前备份当前应用和最新偏好。安装后及验收结束按集合语义比较所有偏好，无变化；保留用户已选的深色／渐隐双弧／圆环模式、105%、5 条、3 天及最新屏幕位置，没有回写旧备份。关注集合的编码顺序变化不算内容变化。
- 最终仅一份真实进程，设置已关闭并收回深色双弧，没有 Demo。原 StatusRing 与已提交版本逐字一致，WindowController 无差异；独立审查确认范围、对称几何和所有旋转角的 2.2pt 留边。代码和文档完成后按用户持续授权提交推送。

用户尚未确认 build 36 的最终视觉满意度，保留实际应用供其查看。设计预览及放大图不代替实际窗口验收。

## 边界

本轮不修改窗口开合、阴影、模式切换、跨屏拖动和任务数据逻辑；不使用新圆环验收代替这些功能的完整回归。连续动画性能／FPS、物理拔插合盖和 Intel 真机需独立证据。原始截图与真实任务标题不提交，Codex 数据只读，公开 Release 附件不变。
