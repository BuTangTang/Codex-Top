# 圆环原位变形与状态动效

2026-09-10，记录 M2 圆环的连续检查点。以下旧产物与旧交互均为当时记录；当前修订、最新产物和待验边界见文末 D-23–D-28 补充。

## bcea145 检查点产物（已由下方修订替换）

- 13:02:43 `swift test`：27 项通过（18 核心 + 7 几何 + 2 状态汇总），0 失败。
- 最新 release Demo 构建、Info.plist 与严格签名校验通过；已替换桌面包并重新打开。
- 桌面 Demo 二进制 SHA-256：`917c44fc5dba4025d14017687b6380647c27b42e47f0a30a51db088a7a8d1786`。
- 较早 12:57 的同窗口变形版本也通过 universal 构建和 ZIP 校验；最后增加 Escape/悬停保护后，桌面使用最新 arm64 包，不能把较早 universal ZIP 当成最后这个二进制。

## bcea145 当时已实测

- 圆环为 44×44pt 单细环，中心点、底层双环、明显阴影已去掉。
- 点击后仍为同一个“Codex Top 悬浮任务”窗口，内容由圆环切换为 80% 六项任务面板；AX 中不再存在圆环按钮。收回后任务行消失，只剩圆环按钮。
- 右上角收起按钮可反向收回；最后版本 Escape 同样缩回圆环，观察端点保持收起。
- 靠屏幕右侧展开后内容可见；本次展开/收回前后保存的圆环位置一致，未被展开后的面板大小覆盖。
- 最新版本圆环向左 100pt / 向下 40pt 拖动后，保存位置按相同距离变化；反向拖回后恢复原值。
- 再向屏幕顶部拖动释放，窗口隐藏且 placement 变为 menuBar；⌘T 显示相同 6 项。随后切回圆环并重启，恢复保存位置和 6 项选择。
- 运行蓝弧的两次实机截图角度不同，确认是持续转动；待回答琥珀环两次截图明暗不同。
- 完成场景显示绿色细环；动画预览切到出错后显示红色细环，状态文字和需要处理计数正确。
- 演示预览通过正常 TaskStore 刷新路径改变合成状态；完成事件逻辑也接在该路径。运行同时存在待处理/失败时，汇总保留运行数量，同时优先提示需要处理。
- 重启新版后仍是深色 + 80% + 圆环；已恢复全部 6 项合成任务及原始示例状态（2 运行、1 待处理），未留下强制出错/完成预览。

## bcea145 验证边界

- 截图工具主要捕获端点。单窗口结构、尺寸变化、收回和状态动画有实际观察，但没有逐帧录像或真实 FPS 数据；不能声称所有中间帧已验收。
- 连续 hover 进出、系统减少动态效果、物理拔插/合盖仍待对应实机测试。
- 完成绿色合拢的完整 0.45s 时间线、混合任务中新完成事件的短暂绿色反馈尚未逐帧抓取；终点显示与汇总规则已检查。
- 同窗口版本基本拖动/吸附已回归；跨屏边缘拖动、屏幕拔插时正在变形等组合还未覆盖。

此前界面基线见 [M2](M2.md)，真实数据与构建限制见 [M3](M3.md)。

## 1757e99 修订与当时桌面产物

- 33 项测试通过，0 失败；实际应用和 Demo 均重新生成 universal 应用/ZIP，包含 arm64、x86_64，严格签名及 ZIP 校验值通过。两种模式的可执行代码一致（各自 bundle 签名不同）。
- 桌面 Demo 与 `dist/Codex Top Demo.app` 二进制完全一致，SHA-256：`02fc0909da9b0383566f4fd817ed87c536dfeeea12b0bc80a8b48347399d2e75`。
- 完成区状态改为共享 Binding，内容和外窗高度读同一值。实测全部完成时：圆环展开完成区 → 常驻浮窗（独立保持折叠）→ 圆环，完成区仍展开，内容与高度一致。
- 取消变形时同时恢复原位圆环的局部坐标、窗口、动画目标和焦点；打开设置/选择器取消待执行悬停，回调到期时再检查模式和对话框。这两项有源码路径检查，屏幕重排时的精确 0.22s 中断和 180ms 悬停竞态仍未实机触发。
- 实机额外发现：原始 6 态 → 展开已结束 → 设置里预览全部完成，汇总已变 6，但部分行仍保留旧运行图标，等待多个刷新周期仍不更新。修正为单个稳定任务 ID 列表，每行直接观察当前 Store。
- 最后 release 包按相同路径复测：6 行全部更新为“已完成”，不切换模式、不重建窗口；再次预览“运行”，所有行恢复运行状态与计时，已结束入口消失。Escape 缩回蓝色单细环，运行数量为 6。
- 最后恢复原始 6 项合成状态，关闭设置。当前桌面为圆环、深色、80%，2 运行、1 待处理；未留下强制预览。

以上为实际窗口与端点状态验收；没有新增 FPS、完整 hover 或物理拔插已通过的结论。

## 945661a 桌面交付检查（历史产物）

后续窗口恢复与设置修订后，完整测试增至 37 项，实际应用和 Demo 均重新打包为 universal，严格签名/ZIP 校验通过。桌面 Demo 与 dist 二进制一致：`bf10880ca635a19fbb3efe4b400cdc15a02ae0eea6e4282732442113594eaed9`。重新启动显示“待处理，2 个运行中，1 个需要处理”，保留深色、80%、圆环和 6 个示例。

该修订不改变细环绘制与动效参数；窗口找回、登录项状态及集成链路证据见 [M3](M3.md)，全量缺项见 [验收对照](acceptance-matrix.md)。

## 点击、主题与数字圆环修订（D-19–D-21，历史检查点）

- 已移除圆环悬停展开/离开收回的计时器；仅点击展开，移开保持，外部点击/收起/Escape 反向缩回。顶部模式仍保留悬停规则，常驻浮窗不监听外部点击。
- 外部 mouseDown 同时采用本地/全局监听，监听仅在圆环展开时存在，取消/模式切换/屏幕恢复均移除；本地返回原事件。另一代理只读复核正常生命周期和内部按钮判定。过渡中的透明额外画布不额外转发点击，不能将其说成穿透到其他应用。
- 在点击展开修订中已实测：点击打开，Esc 收回，圆环左移 80pt/下移 40pt 后不展开，反向拖回；展开面板空白标题区域按下拖到面板外后保持。后者是拖动越界，不是无按键 hover 验收。
- 最新桌面包实测：44pt 黑色圆环显示中心 2；点击打开原来的 6 项列表；主题按钮切到浅色后面板保持展开，浅色圆环中心仍为 2，再展开可切回黑色。浅色材料、文字与状态环配色均更新，内部主题点击正常。
- 试图用访达验证外部点击时，AX 按钮动作切换了标签页，但它不是实际 mouseDown；后续鼠标坐标操作前访达窗口被关闭，工具返回 noWindowsAvailable。没有据此声称真实跨应用鼠标点击已通过；用户同时在操作桌面，后续停止 UI 干扰。
- 圆度采用 circular 路径和屏幕像素对齐；保留单 1.8pt 描边。数量为关注列表的运行数，状态优先级未改变，数字不旋转、不呼吸，大数量为 99+。完成/错误等其他状态的数字组合仍需更多实机验证，既有核心汇总测试继续通过。
- 13:55:56 测试 37 项、0 失败；之后仅增加中心显示，再次完成两模式 universal release 构建。Demo 已复制桌面，二进制 SHA-256：`2ec5cde5c3405fd461ec75e4d9bfdcba744cbe9dc0ef09afd75d8544b2517a50`。
- 主题过渡实现为固定黑/浅玻璃层、显式颜色端点和太阳/月亮图标过渡，常规 0.26s、减少动态效果 0.10s。截图只证明端点，未测真实 FPS、完整中间帧或系统减少动态效果。

## D-23–D-28 当前修订与独立 Animation QA

本节覆盖后续径向主题、圆环菜单与拖动、字体、刷新稳定性和整球呼吸。此前圆环自动吸附、状态环透明度呼吸和 0.26s 整体主题过渡属于历史行为，分别由 D-24、D-28、D-23 覆盖。

### 源码修订与边界

- D-23：从实际点击点向外圆形揭开新底色、文字和控件，常规 0.42s；减少动态效果为 0.10s 短淡变。正常浅色过渡保留原生玻璃；快速打断浅色过渡时采用近似玻璃合成，不声称逐像素连续。模式、尺寸或 backing scale 改变取消旧覆盖层。
- D-24/D-25：圆环拖至顶部不自动吸附，右键复用完整状态栏菜单；仅常驻浮窗保留顶部吸附。系统状态栏计数移除黑色胶囊，文字跟随系统明暗，彩色点保留；摄像头黑底不变。
- D-26：标题 17pt、任务名 16pt、辅助说明 14pt、搜索 16pt；窗口逻辑宽高、缩放比例、圆环数字与系统状态栏字号不变。
- D-27：`WindowGeometry.pixelAligned` 将展开目标按屏幕 backing scale 对齐；同一目标允许 1 个物理像素误差，避免定期刷新重复播放变形。`expandedSize` 和 `surfaceFrame` 使用相同曲线与展开/收起时长，提示出现时内容和外壳同步。新增几何回归覆盖 1×/2× 屏幕的缩放提示端点。
- 旧版本未记录运行时 frame 差值，因此像素误差是源码分析得到的候选原因，未被记录为已抓到闪空根因。
- D-28：仅有需处理事项、圆球收起且未启用减少动态效果时，整体 1.00↔0.94 缩放，每半程 0.9s，直径约 44↔41.4pt。原生窗口保持 44pt；展开、处理完和减少动态效果时停止，中心仍表示运行数。状态内环不再叠加透明度呼吸。

### D-27 独立包实际观察

使用独立 Animation QA 包和 6 项合成任务，浅色主题、80% 比例；这些记录不是最终 D-28 包实测。

- 点击 Demo 任务行出现提示后，连续两张跨越 2 秒刷新周期的截图中内容与窗口尺寸稳定，未观察到整块空白。
- 浅色切到深色的端点文字可读。
- 右键收起圆球出现完整菜单。
- 向屏幕顶部拖动圆球后，偏好仍为 `placement = orb`，保存纵坐标约 `0.99377`，没有自动吸附。

这些观察只覆盖列明端点和刷新后的稳定状态，没有逐帧、FPS 或全部中间帧通过的结论。系统减少动态效果、快速打断玻璃合成及 D-28 呼吸停止条件未据此标为实测通过。

### 最终产物与交付边界

- 2026-09-10 14:20:59：38 项测试全部通过，0 失败（24 核心 + 12 几何 + 2 状态汇总）。
- 最终 D-28 Demo 和实际应用均完成 arm64 + x86_64 release 构建、严格签名及 ZIP 完整性校验。Demo ZIP SHA-256：`01563e073fabe6e0ea729f6d445ee6553e54b75f863d04c1eb2e197cb36c4c7f`；实际应用 ZIP SHA-256：`6b64b4d87916eec10558fc0779988215542d04dbd0e4fd74e7d699bce82d7fca`。
- 独立更新 Demo 已复制到桌面的 `Codex Top 更新` 文件夹。该副本与 dist 可执行文件一致，二进制 SHA-256：`92b6393fb92913a7ab56d80562593af857ed65b92d2a9e589d127cefb095a2c8`；未启动新版，未覆盖正在运行的旧包。
- 用户询问 Computer Use 操作过多后，本轮停止桌面操作，只继续代码检查、构建和文件交付。最终 D-28 包未启动实测；交付后需用户退出旧版再打开新版，不覆盖或重启用户正在运行的旧包。
