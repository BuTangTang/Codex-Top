# 1.0.0 显示模式与列表高度验收

日期：2026-09-14。正式版本号 1.0.0，构建号 7。

## 交付内容

- 监控面板右上角增加一个“显示位置”入口，点击展开四种模式，当前项打勾；悬停说明与辅助功能值提供当前模式。
- 深色主题使用白色入口，浅色使用深色；保留图钉、任务选择、圆环收起及浮窗关闭按钮。
- 菜单跟踪期间暂停圆环外部点击收起和刘海离开收起，菜单结束后恢复，防止来源面板先收回而菜单仍独立悬着。
- 置顶前记住原模式，取消图钉或关闭浮窗返回原模式，重启仍保留；重复置顶不覆盖来源，旧文件缺记录时回退刘海。
- 设置最上方为“显示位置”；“任务 → 可见任务数”可选择 1–12 条（默认 4），控制列表高度，超出滚动，四种模式共用并保存。
- 默认构建、ZIP 命名和当前说明同步到 1.0.0；历史版本及截图来源不改写。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 测试及构建 | `swift test` 122 项通过；`swift build -c release`、`scripts/package.sh --universal` 通过 |
| 包与签名 | 最终包包含 arm64 / x86_64，严格签名检查通过；生成 `Codex-Top-1.0.0-macOS.zip` 及 SHA-256 文件 |
| 显示菜单 UI（build 4） | 同一源码的 arm64 打包实例在外接 Q27G10E2 核对四种模式：圆环 → 常驻浮窗 → 刘海 → 仅状态栏 → 圆环 |
| 菜单关闭 | 圆环展开后打开菜单，Escape 取消菜单且无独立残留；选中其他模式后菜单关闭，目标模式显示正确 |
| 状态栏返回 | 切入仅状态栏后桌面面板消失，用应用既有 `⌘T` 打开其任务弹层，再从新菜单返回圆环 |
| 图标与布局 | 深色白图标、浅色深图标可见；60% 圆环展开及浮窗、75% 展开面板、120% 状态栏弹层无标题与新入口遮挡 |
| 偏好 | 收尾保留用户当前显示选择、深色和 75%；原关注项保留，期间自动加入的新任务保留；排除及自动关注基线另按集合核对 |
| 取消置顶 | build 5 通用包核对圆环 → 置顶 → 重启 → 取消返回圆环、仅状态栏 → 置顶 → 关闭返回仅状态栏、刘海 → 置顶 → 取消返回刘海；build 7 再验刘海返回 |
| 设置顺序（build 7） | 真实窗口首区为显示位置，其后外观和任务；可见任务数菜单提供 1–12 条 |
| 列表高度（build 7） | 外接屏 75%：2 条时刘海展开和紧凑浮窗均变矮，刘海滚动可看到超出任务；改回 5 条后，4 个活动任务按实际行数收缩，展开已结束后显示 5 行上限 |
| 条数保存（build 7） | 从 build 6 退出再启动 build 7，设置读回用户已选的 5 条；测试后再次恢复 5 条 |
| 安装 | 最终 build 7 通用包严格签名和 ZIP 摘要通过，安装程序摘要与 dist 一致，唯一真实实例，设置和已结束区已关闭 |
| 程序 SHA-256 | `839c625facc6a1e481bcb3f359334416fa1c27bab8869bcfbf98f2de4dbb8405` |

没有启动 Demo，没有提交真实任务标题、对话、偏好或诊断原始数据。菜单勾选由原生 Picker 提供；Computer Use 无法截取原生菜单本身，菜单选项、选后模式及关闭行为通过辅助功能与真实窗口核对，不宣称取得菜单截图。

本轮只提交源码、更新本机应用并生成本地 ZIP，没有新建或覆盖公开 GitHub Release 附件。最终列表高度实机覆盖刘海和常驻浮窗；圆环与状态栏共用高度计算，但未在 build 7 逐档实机重测，1/12 条的屏幕极限高度也未实测。未重复验收物理拔插、Intel 真机或完整动画帧率；旧待处理延迟的未定位边界继续见 [长等待复查](waiting-navigation.md#2026-09-14-长等待复查)。
