# 对话顶栏与统一账号验收（2026-09-25）

本批落实用户选择的方案 B（状态在顶部、底部只留输入框），并按用户要求将电脑与模拟器切换到同一个正式账号。沿用现有消息、状态与控制来源，不新建接口、轮询或键盘布局 owner。

## 账号与电脑连接

- 管理员预建账号最低密码长度按 D-15 从12位改为8位，上限1024不变。6项真实临时 SQLite 集成覆盖边界、既有账号不被覆盖，另6项邻近回归和服务端类型检查通过，独立复核通过。
- 云端通过既有管理员标准入口创建用户指定账号，登录封套解密与服务端身份回读成功；不记录密码、令牌或原始认证输出。
- 正式 Mac 通过产品退出／登录切换，旧连接状态先作本机私有备份；不修改原 Codex 数据、认证或历史。新账号登记自己的电脑身份，原测试账号及其云端资料未删除。
- Android 模拟器通过正常账户页退出并登录同一账号。覆盖安装保留新账号、最近50条设置和应用数据。
- 独立只读核对：Mac 当前账户与服务端 profile 一致，当前电脑属于该账户且在线，连接进程运行正常；一次首页读取50条、存在下一页、没有不完整标记，658ms。

云端本次只更新运行容器内管理员开户源文件并完成开户，基础镜像未重建；旧镜像重建会恢复旧开户长度规则，已有账号的正常登录不受影响。后续发布镜像需包含本批服务端提交，不把运行容器补丁称为镜像发布。

## Mac build48

正式包1.0.0 build48，原生 SHA-256 `3a11bf8023925e9b48276aaaca8675f47242e150db4a67026887ad0cc3ff247a`；连接组件 `1.0.0+codextop.48`，指纹 `513daf2fcf20b4f8`。

修复从安装的 Codex 官方应用定位可执行 CLI，并通过现有环境项交给连接组件；目录和不可执行文件不作为有效结果。此前 LIST 降级造成的不完整提示在本机同一路径消失。Swift205项、release、连接组件构建、严格签名核对及独立审查通过，已覆盖正式应用并核对实窗，无并行 Demo。抽样首页读取通过不代表所有历史已完全覆盖。

## 顶栏实现与源码验证

- 手机继承桌面设置的会话采用单行标题、单行来源与真实状态，异常及待处理进入详情。原始审批组件、权限、请求版本与提交锁定保持；切任务／离页时清理弹层，打开期间更新现有详情。
- 顶栏高度仅随平台与字体设置改变，不随生命周期或错误文字改变。Android 默认48+48dp；2.5倍字体测试为72+58dp，实际触控框不超出父行。状态切换一次160ms淡入，尊重减少动态效果。
- 生命周期与操作可用性分开：控制暂时失败不覆盖仍可信的运行事实；缺失或过期仍明确未知。详情保留完整说明，平板和网页沿用原展示。
- 首批相关4文件138项、全UI类型检查通过；字体问题先复现失败再修复，最终相关23项及类型检查通过，独立真实Text尺寸4项通过。多轮专项有重叠，不累加为总用例数。
- 额外核查发现原 machine-offline/not-resumable 提示独立经过 ChatFooter，首包未完全收起；已移入同一顶部详情。实测超特大字号时状态被来源挤成省略号也已修正，来源先让位、短状态完整保留。最终两文件125项、全UI类型检查通过；独立7项核对恢复、换任务清理、平板／网页兼容及320dp大字号宽度，未发现新增阻断。最终包见下文收口记录。

## 模拟器连续操作

1.0.28/code29 已以原签名覆盖安装，APK SHA-256 `bf89dbbf535c9fa65244ea0ddcd2ab4ced2d8b016787d11b74a21837dec40f44`。该包是顶栏首轮验收包，尚缺上述离线提示迁移。

同一正式账号在手机向指定原桌面测试任务发送一次有界等待请求，原任务32.538秒完成；手机实际观察运行中→已完成并收到一条对应回复。两态在1080×2400模拟器中标题／状态区域[128,380]、输入操作区域[2127,2253]一致，空草稿保持。顶部详情可打开／关闭、完整显示来源与可发送说明，未再展开原控制错误卡片。

## 最终包 1.0.29

正式发布构建1.0.29/code30，APK SHA-256 `8fcaff478250b4767e8487f40c2638c5f79c8e827dad71c8a27c43c9b64de2d7`；仅arm64，原包名、签名与服务器配置保留。增量prebuild与assembleRelease通过，构建3分34秒，最终源码哈希与审查版本一致；已覆盖安装模拟器。

实际暂时停止正式电脑连接（未停止Codex会话／退出账号），手机手动刷新后显示未知与顶部详情，详情可查看原控制错误及重试；标题／状态区域仍[128,380]、输入仍[2127,2253]。恢复同一连接后，当前已打开详情自动恢复已完成与可发送，无须关闭重进；前后认证账户和电脑身份相同。此轮实际覆盖控制不可读的说明；独立 machine-offline/not-resumable 文案分支由真实组件测试覆盖，不冒称现场也出现了该文案。

最终包再次使用“超特大”文字设置查看同一任务，未知与已完成均完整显示，长来源省略，标题／刷新／输入未被覆盖。默认字体、跟随系统主题及原拼音输入法均已恢复。深色、键盘开闭和状态详情在1.0.28首包已实际查看，1.0.29仅追加上述已审查的提示迁移与宽度修正；不重复扩大版本覆盖口径。

手机／服务源码提交 `fa1171abd63b3f64d7eee5ecf1cfb5fc9fe1bd3d`；自有远端快照 `48b7e8e9a22ab85d6d6d060367f750b95f6c9ea5` 已普通推送。快照仅排除原上游37个自动化工作流，其余树与源码提交相同；本批仅8文件，未推送上游仓库。

## 已知边界

- 当前主管长任务在首页仍为未知，本批没有声称修复全部长任务状态。
- 发送后立即收键盘、期间未拖正文，完成回复曾停在尾部以上120px而部分遮住；手滑到底可完整阅读。独立只读审查确认布局可滚至正确底部，尚不能归因于本次顶栏；不硬加高度补偿。此路径仍需原跟随逻辑专项排查，不称自动跟随全部通过。
- 只有 Android 模拟器连接，本批未安装小米。真实审批、锁屏推送、多电脑和外接屏等既有缺项保留；组件审批测试不代替真实审批验收。
- 未操作 PVTC。本批图片为设计参考，真实账号／任务正文截图只保留本机私有验收，不提交公开诊断。
