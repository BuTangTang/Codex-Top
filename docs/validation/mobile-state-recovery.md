# 手机状态与项目导航验收（2026-09-25）

本批覆盖用户反馈的旧任务状态未知、运行中仍显示完成、桌面控制读取失败，以及 D-12 最近数量、D-13 自动打开已有任务、D-14 电脑所属子页面。以下区分已通过路径与仍在处理的边界。

## 当前安装

| 产物 | 版本 | SHA-256 |
|---|---|---|
| Mac 原生程序 | 1.0.0 build47 | `0db403e34fb00146c524cd2a67b5ced9bd5efc3ac6388546b278add048e2412f` |
| Android APK | 1.0.27 / code28 | `b337c99b0222f0bf5b235ffcae2c7efbb57ee3fef6b3739619280dc0fc367a16` |

Mac 连接组件 `1.0.0+codextop.47`，指纹 `49b93c5f4a94dea5`，正式目录账号、机器与偏好保留。Android 正式 release 构建2分40秒、同签名验证与模拟器覆盖安装通过，版本回读一致；原账号及应用数据保留。仅模拟器，不代表小米已安装。

1.0.25/code26 是首批分页／时钟包；1.0.26/code27 完成 D-12/D-14 与收发验收；1.0.27 仅补项目主副标题纵排，正文与收发实现不变。1.0.26 APK SHA-256：`113e6d968be52445d57d32ab0018d9346ebf0aa00e1d1d87ff31ec845a98e446`。

本批手机与连接组件源码提交 `ffac29278d9bf0c4117bafc941493cc5ad2ec00e`；自有 Codex-Top-Mobile 仓库快照 `31b089eed88e20ac005c59a3193bcb1f3be80387` 已推送并回读一致。快照仅排除上游自动化工作流，源码与本次提交一致。

## 列表状态修复

旧任务尾窗的前轮不完整上下文曾提前结束读取，漏掉后续明确开始与完成；晚到的子任务活动会干扰主轮。正常含图片长任务中，当前轮开始距文件尾超过12 MB，原64 KiB尾窗和8 MiB冷读不能覆盖。现采用有界冷读与增量缓存，保留真实轮锚；同轮原生继续事件恢复运行，审批仅由明确解决事件清除，不按mtime、最近活跃或创建时间猜状态。

手机仍用原分页owner：首屏新事实即时更新，未重读尾行撤销生命周期，完整重建成功才替换成员和游标；失败保留已读范围并禁用旧游标。共享前台分钟节拍和最近过期点负责重绘，失焦／后台清理，不设逐行定时器。

生命周期／LIST139项、独立22场景、正常12/16 MiB图片行与CLI类型检查通过。分页／时钟相关92项与71项通过，存在重叠不相加，另经独立11项复核。主仓Swift203项与release构建通过。

正式公网LIST当前主管任务running、旧UI任务completed；模拟器目视一致。用户指出的两条旧任务列表已显示完成，取消任务仍为取消，不把整个历史的未知统一改为完成。

## D-13 已有任务自动打开

当前原生版本26.917.62051 (10789) 下，notLoaded任务没有可用owner。指定QA的socket初始化4ms，owner发现4999ms超时，尚未进入历史快照阶段；经官方入口打开同一任务后，owner发现1ms、控制读取446ms，返回completed/start。这确认该路径的未加载问题，不能推广成所有控制错误都由此引起。

自动打开只挂在明确LINK流程：校验规范来源与已有任务身份；正常已加载任务不导航，未加载只调用一次官方入口并有限重查。后台STATUS／CONTROL／续租不切页，不新建或接管任务。旧导入会话复用兼容保持原关联和运行器。

67项单测、10项LINK集成、全CLI类型检查及独立复核通过。Mac46 + Android1.0.26实测：旧UI任务打开前原生notLoaded；仅由手机点击后原生变idle，手机加载原对话、显示完成，无控制错误，没有向此任务发送消息。

## D-12 最近数量

最近列表默认50条，“我的”可选20／50／100／200；全部电脑聚合后统一限额。调整上限作废旧分页，新会话加入前面，阅读时保留已有行顺序与位置。项目历史继续独立50条分页，不受首页上限截断。

相关135项、独立8项与UI类型检查通过。模拟器实际默认50，选择20后退出重启仍保留20；升级1.0.27仍保留该设置，验收后已恢复50条。100／200跨机器与旧响应失效以专项测试为证，不冒充多电脑实测。

## D-14 所属子页面

电脑详情→该电脑全部会话／所选项目会话→具体对话，返回逐层回原来源，不导航到全局“会话”入口，不改首页过滤。项目名在上、电脑名在下居中，长名省略，沿用原生转场与正文路由。

相关58项、电脑／项目入口8项、标题55项及最终UI类型检查通过，部分重叠不相加。独立使用已安装Expo真实导航reducer验证4种旧会话栈和12次连续返回。

1.0.27实际检查两个不同项目：标题及项目会话范围正确；打开具体对话后返回各自项目，继续返回电脑详情及原电脑入口。项目列表点加载更多追加更早会话，返回保留已读位置。1.0.26发现主副标题横排，已在1.0.27修正并目视复验。部分列表仍显示覆盖不完整提示，但有效游标可继续加载；不把当前页匹配数当该项目全部历史。

## 手机收发

Mac46 + Android1.0.26仅向指定QA发送一个有界等待测试。手机实际显示运行中，原桌面同一任务产生新轮，41.065秒后完成；手机收到一条对应测试回复、已完成且草稿为空。没有新建／接管任务。最终1.0.27再次打开该QA正常恢复正文与完成状态。

此前1.0.25发送20.6秒后完成的路径仅采到完成画面，本批已补实际运行中画面；不以最终回复代替过程状态验收。

## 首次状态恢复修复

跨项目打开另一旧任务时，原生已idle，但手机首次CONTROL失败；随后单次只读探针owner1ms、完整控制2574ms、completed/start。手动刷新消除控制横幅后底部仍未知；正式STATUS返回online/idle/canSend:true及unknown/not_observed，确认上游状态观察仍未恢复。

源码查到首次租约基线失败后baselineStarted保持true，后续按需读取不能再恢复。原租约已补最多一次恢复，不新增轮询或用LIST替代控制。33项回归及CLI类型检查通过，独立来源边界复验3项、额外恢复与终态4项、手机观察变化后自动重读CONTROL1项通过（有重叠不累加）。首次真实5秒无应答可合成复现旧版持续未知，修后无新正文也可恢复已完成。真实STATUS与此缺口吻合，但无法仅凭该reason排除租约缺失，不能写成已证实本次一定是超时。build47已完成release／连接组件构建和签名验证，覆盖正式包并启动同账号同机器daemon，控制可达与版本一致。手机重新打开同一IM旧任务，未经手动刷新即显示已完成，CONTROL横幅消失；正式STATUS单次233ms也返回completed/desktop、canSend:true，实际复验通过。本次重启创建新租约，因此“第一次失败后同租约自动恢复”以33项中的真实超时合成测试为证，不冒称现场再次注入了失败。

## 边界

独立CONTROL首次瞬态失败而STATUS观察始终不变时，仍沿原手动刷新处理，本批没有扩展手机定时重试。进入页面等待同步期间可能短暂未知；不把这种初始状态写成瞬时加载已实现。


PVTC与云端部署未操作。真实审批、小米、锁屏推送、多电脑、外接屏及完整历史覆盖仍按原任务保留，不称REQ-002整体完成。状态缺失、过期及确实无法判断时仍明确显示未知。
