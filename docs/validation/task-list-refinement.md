# 紧凑列表、手动整理与显示比例验收

日期：2026-09-20。交付：v1.0.0 build 18，D-70 / D-71 / D-72。

## 最终行为

- 停止/完成任务单行，活动与异常状态保留双行；结束任务名减粗，字号和宽度规则保留。
- 只在运行区域末尾与其他任务之间保留一条淡线；其余任务之间无逐行分隔线。分组线随内容滚动，并按一个物理像素处理。
- 结束区折叠与额度合并底栏；暂停、缺失与过期状态仍可识别。
- 右键“移出监控”只取消本应用关注；原任务保留，手动排除不会因刷新或重启回流，“＋”可重新加入。
- 选择器“取消当前结果”只修改当前筛选命中的草稿，确认后保存；取消放弃草稿。
- 新 100% 等于旧 75% 的实际尺寸与字体，范围 80%–120%，每 5% 一档共 9 档。恢复按钮与应用快捷键使用同一规则，圆环仍为 44pt。
- 打开设置、操作比例或主题时保留已展开面板与结束区状态。点击其他应用、手动关闭等原有退出方式仍保留。

## 滚动问题与回归

使用独立临时数据库、偏好和 100 条合成任务，真实 MonitorView 加载到隐藏 NSHostingView；所有测试窗口始终不可见。旧 LazyVStack 在内容/状态不变的往返滚动中，总高度依次为 4007、3940、3913、3824、3819、3832 点，变化 188 点。固定行高并不足以避免懒加载估高修正。

改成 VStack 后，相同回归要求所有采样高度变化不超过 1 点、回到顶部为 0，测试通过。没有通过更改字体或遮盖分隔线代替修复。

另外覆盖移出后刷新/重启保持排除、手动恢复、批量取消保留运行任务，以及源数据库内容不变。比例回归覆盖旧 75% 编解码后实际尺寸不变、9 档及中点、上下界、重复快捷键与几何安全范围。

## 构建与包

- build 17：131 项测试通过，release、arm64/x86_64 通用构建与严格签名通过。
- 最终 build 18：133 项测试全部通过，release、通用构建、严格深度签名及 ZIP 完整性检查通过。
- 最终安装程序与打包程序 SHA-256：`38d4a4ea972b15623c9d283587dea01447121853f366e51341f0fae5b968c54c`。
- 本地 ZIP：`Codex-Top-1.0.0-build18-macOS.zip`，SHA-256：`1e81ee7f41d9233b6ef2ee9c6f687ee978517a850ba49e926016cff17ce701a6`。
- 安装到系统应用程序目录，保留唯一真实实例；没有运行桌面 Demo 副本。本轮不发布或覆盖 GitHub Release。

## 真实窗口核对

build 17 已核对：

- 外接屏浅色旧 75% 的运行/结束混合列表，连续多次向下、向上返回顶部；文字对齐与分组线位置保持。
- 内置 Retina 屏相同比例的往返滚动，返回顶部位置为 0，图标/标题/状态相对位置保持。
- 右键停止任务移出后计数减少，选择器仍可搜索原任务，重新勾选确认后恢复；来源任务未删除。
- 选择器筛选已结束、取消当前结果，切回运行筛选仍保持运行任务选中；取消草稿后关注列表恢复原样。
- 圆环、常驻浮窗、刘海、状态栏展开面板；常驻深色旧 60%/120% 和旧 60% 选择器布局，未见新增按钮挤压。

最终 build 18 已核对：

- 读取原 75% 偏好后设置显示 100%，滑块端点为 80%/120%；外接屏展开面板仍为约 308 × 217pt，与改基准前一致。
- 设置窗口打开后面板保持展开；原生滑块设置为 80% 后面板为约 246 × 174pt，未收成 44pt 圆环；设置窗口自身保持 500 × 610pt。
- 滑块 120% 标签同步；快捷键减少一次变为 115%；点击恢复按钮后标签/滑块回到 100%，展开面板回到约 308 × 217pt，结束区仍展开。
- 真实窗口与合成回归相互补充；build 17 的四模式/内置屏操作没有冒充 build 18 的逐项重测。

## 保留与边界

任务操作验收前后，关注、排除与自动基线集合一致；build 17 多模式验收后恢复原显示偏好。用户在后续讨论中自行调整的位置，以 build 18 比例验收开始时的最新偏好为准，比例恢复后全部偏好语义一致，不覆盖该位置。

保留设置和已展开预览，方便用户继续调整。唯一 Computer Use 会话在结束时清理。真实任务截图仅用于当前本机验收，不提交真实标题、原始日志、偏好或桌面内容；公开设计图只使用合成标题。

滑块连续鼠标拖动本轮工具尝试未改变值，不能计为成功；已通过原生可访问性值、快捷键和恢复按钮验证刻度、布局及预览保持。物理拔插/合盖、Intel 真机、完整动画逐帧/FPS 和超大任务集合性能没有本轮实测结论。旧 60%–120% 与 build 10 截图继续作为历史，不代表当前显示比例。
