# M1 数据接入验收

日期：2026-09-10。环境：Apple Silicon、macOS 15.7.9、Swift 6.1.2、Codex CLI 0.153.4。

## 自动验证

- `swift test`：16 项测试通过，0 失败。
- `swift build -c release`：通过。
- 测试覆盖开始/结束/继续、迟到的旧轮次结束、同步与异步提问、过期运行状态、乱序与损坏 JSON、半行、截断/替换、超长历史、配额窗口识别、自动/手动选择、子孙任务/孤儿/循环关系、设置损坏、只读 SQLite、归档过滤、越界路径和不支持的数据库。
- 合成 SQLite 测试比较读取前后的数据库字节，保持一致。

## 本机只读诊断

诊断只发布汇总，不保留原始任务数据。读取 726 条本机记录，聚合为 194 个主任务；识别到 1 个运行任务，和当时本开发任务正在执行相符。其余记录为完成、停止和未知，不将未知补成完成。

初版冷读取 180.8 MB / 13.56 秒；改成 64 KiB 有界尾部和轻量日期解析后，release 冷读取 47.3 MB / 0.85 秒。随后的无变更刷新读取 0 字节。优化前后这批数据的状态分类一致。这是本机测量值，不是所有机器的性能保证。

日志只返回周窗口时，仅识别周额度，没有把它误认成 5 小时窗口。

## 尚未证明的内容

M1 不代表图形界面、物理显示器拔插或所有 Codex 版本兼容性已经通过。批准/输入状态测试使用明确事件的合成数据；当前未证明所有桌面批准方式都会产生可识别的日志事件。实际界面和支持边界将在 M2/M3 验证。
