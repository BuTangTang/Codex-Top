# M1 数据接入验收

日期：2026-09-10。环境：Apple Silicon、macOS 15.7.9、Swift 6.1.2、Codex CLI 0.153.4。

## 初版检查点自动验证

- `swift test`：16 项测试通过，0 失败。
- `swift build -c release`：通过。
- 测试覆盖开始/结束/继续、迟到的旧轮次结束、同步与异步提问、过期运行状态、乱序与损坏 JSON、半行、截断/替换、超长历史、配额窗口识别、自动/手动选择、子孙任务/孤儿/循环关系、设置损坏、只读 SQLite、归档过滤、越界路径和不支持的数据库。
- 合成 SQLite 测试比较读取前后的数据库字节，保持一致。

## 2026-09-10 状态与自动加入回归

13:20:18 执行 `swift test`：全包 33 项通过，0 失败，其中核心测试 24 项、几何 7 项、状态汇总 2 项。此次新增 6 项核心回归，均使用合成事件或内存偏好，不读取真实 Codex 数据：

| 回归 | 输入与证明范围 |
|---|---|
| 终止事件轮次隔离 | 完成、取消、失败共 7 种事件；旧轮次事件不改变新轮次状态或活动时间，匹配当前轮次的事件仍生效 |
| 失败保持 | 失败后追加助手说明、reasoning、工具调用/结果、提问或完成事件，仍为失败；额度事件可以独立更新 |
| 失败后明确恢复 | 新轮开始、用户消息、用户输入、批准结果和用户消息 item 可恢复运行，并重设计时/轮次信息 |
| 同秒启用与开关 | 起点在整秒后的 0.25 秒，新任务创建时间为同一整秒；已知旧 ID 不加入，新 ID 开始后加入，未开始不加入，关闭/重开及手动排除保持约定 |
| 首扫与重启基线 | 首扫不加入同秒历史完成项，序列化重启后保留基线，并允许同秒新 ID 自动加入 |
| 旧偏好迁移 | 缺失 `autoBaselineIDs` 仍可解码；同秒旧记录保守留在基线，明确晚于起点的任务继续补加，原关注与排除保留 |

`git diff --check` 通过。上述结果证明列出的合成输入行为，不等于全部数据接入要求、真实任务事件变体或图形界面均已验收。

迁移边界：旧偏好没有启用时的完整观察历史，所以不会追认迁移快照里时间早于精确起点的同秒任务。对同一秒内且从未观察过的 ID，数据库的整秒时间无法区分启用前后，策略按新出现判断；已知旧 ID 和手动排除不受影响。

## 本机只读诊断

诊断只发布汇总，不保留原始任务数据。读取 726 条本机记录，聚合为 194 个主任务；识别到 1 个运行任务，和当时本开发任务正在执行相符。其余记录为完成、停止和未知，不将未知补成完成。

初版冷读取 180.8 MB / 13.56 秒；改成 64 KiB 有界尾部和轻量日期解析后，release 冷读取 47.3 MB / 0.85 秒。随后的无变更刷新读取 0 字节。优化前后这批数据的状态分类一致。这是本机测量值，不是所有机器的性能保证。

日志只返回周窗口时，仅识别周额度，没有把它误认成 5 小时窗口。

## 尚未证明的内容

M1 不代表图形界面、物理显示器拔插或所有 Codex 版本兼容性已经通过。批准/输入状态测试使用明确事件的合成数据；当前未证明所有桌面批准方式都会产生可识别的日志事件。实际界面和支持边界将在 M2/M3 验证。
