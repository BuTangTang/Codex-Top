# Happier 手机接入验收

## 1.0.22 公网功能联调（2026-09-25，指定恢复路径复验通过）

- 最终恢复修复仅在有效来源边界后读取一次关联当前快照，等待期间的新事实、断代和释放均优先；后续Desktop换轮须有同一连续尾岛顺序证明。控制73项、联合130项、UI28项、最终类型检查及官方CLI构建通过（不相加）。CLI指纹 `7883a77fc13c70a3`，源码dev `0fed02010`，自有快照 `2b5945d74` 已推送。
- 重新打开静止原会话，正常同步后显示已完成；手机新轮先显示运行中，再退后台等原Desktop约36.2秒完成，回App得到一条准确回复和已完成，无残留提示或草稿。恢复后再次发送约29.0秒完成、单回复与完成状态保持；本轮未再次捕获短任务中间运行帧，不把终态样本扩为逐帧证明。
- 初始恢复采样短暂未知，后续正常同步确认；不能称立即显示或跨断代永久可信。下列早期失败保留复现过程，该指定恢复路径现已通过，其他缺项保持。

- Android 1.0.22/code23，同签名覆盖安装模拟器独立用户，云端账号、电脑与原任务历史保留；APK SHA-256 `8b9d765d18ac20d770aa3148af65df15f3fbaa6ce952b6885965136cd003efd5`。小米未连接。
- 手机发送作用域复用既有服务器身份等价判断；不同服务器仍拒绝。控制读取只采用固定owner匹配请求的历史回执revision，拒绝冲突快照和补丁缺口；长期观察在Desktop未知时采用真实前向生命周期事实。
- 最终控制路径128项、lease/RPC/通知联合112项、UI28项与CLI/UI类型检查通过；计数有重叠。CLI正式构建成功，只更新独立云端QA daemon；运行、机器和控制可达回读正常。
- 第一轮手机发送后草稿清空，显示运行中；同一原Desktop约39.9秒完成后，手机精确回复一条、已完成、无残留受理/等待提示。
- 第二轮在同页直接再次发送成功，运行中可见。随后模拟器退后台；原Desktop约42.0秒完成，恢复App后回复一条、输入为空、无残留提示，但状态持续未知。该恢复场景未通过，不能以第一轮代替。
- 已定位无后台关注时退出前台会释放观察lease；重新订阅从tail建立游标，空档内完成落在新边界之前。继续修复可信当前状态恢复，不依据最后回复、文件mtime或旧缓存生成完成。
- 证据仅私有保存于companion的 `.local/req002-qa/functional-122/`，不提交原始对话、账号或设备转储。PVTC和云端服务配置本批未操作；真实审批、推送、小米、多电脑、冷历史owner兼容仍未完成。

## REQ-002 重启后的本机集成（2026-09-24）

- 补充账号隔离实测：安卓1.0.14冷启动恢复A登录、退出后B登录并显示空电脑／会话列表；同一隔离服务的A电脑、会话、消息和轮次详情均200，B读取这些A资源均404，B自身列表为空。私有脱敏证据为 `.local/req002-qa/account-isolation-readonly.log`；四类服务检查均为GET，未发送消息或审批。此项不能替代原任务收发与通知换号场景。

- 更新：安卓 1.0.14/code15 实际登录成功，参数到登录请求约0.38秒，登录接口200，UI离开表单并读取在线电脑及11个真实项目；选择项目可更新列表。APK SHA256 `40b82d644238f3b49a95bc870071c03a54a9097cef94caba2196e8d9c7ab1d9c`，原签名不变，模拟器覆盖安装回读一致。Android实际provider5组原生密码向量通过。系统通知权限已允许，但缺自有推送配置，不代表送达。
- 用户实测否决当前首页：通知品牌、最近活跃／空闲标签、大块电脑／项目选择等问题已记录；内部子代理、标题污染及worktree过滤缺口已定位。正在讨论首页修订，不能称UI验收通过。原任务发送、通知换号、双电脑和公网仍未在本包完成验证。
- Mac更新：build42已完成201项测试、release打包和签名验证并安装，正式设置与单实例监控窗口正常；隔离daemon经canonical start从0.2.13更新为1.0.0+codextop.42，原生表单尚未用正式账号登录。
- 范围依据需求 v0.10 / 设计 v0.2 / D-05；手机原生新建暂缓。使用独立本机 3011 服务、合成账号 A/B 和模拟器测试用户，未部署到 PVTC，未创建正式同事账号。
- Mac build 41 的 201 项测试、release、独立连接组件打包和签名验证通过，已安装。真实设置窗口中的账号区域与无效地址提示已检查；仅有一个正式监控实例。组件在不含 Node 的基础 PATH 下实际通过登录、退出与重新登录，不等于原生表单或公网全流程验收。
- 安卓 1.0.13/code14 构建与覆盖安装成功，原签名未变；SHA256 `9efba2d6427a7556b026c0046f200d26a21c2b9278e79d346aee323ed739ab3b`。仅安装模拟器，小米不在线。
- 实测未通过：该 APK 参数请求已返回 200，但登录计算超过 6 分钟仍未发起登录请求、进程持续占用 CPU，不能声称密码登录可用。已定位并改为现有 Android 原生模块执行相同 PBKDF2-SHA512；协议向量 6 项、桥接与登录 17 项、原生模块编译通过，待新 APK 实测。
- M3 原任务交互专项 127 项及全 UI 类型检查、M4 路由隔离专项 43 项、注册退出竞争 3 项、SQLite token 归属 3 项通过。它们不替代真实通知、审批执行及连续交互。
- 尚待：新包登录及连续操作、双电脑、原任务审批实际回执、HTTPS 公网与小米锁屏推送。缺可用域名与自有推送配置；Docker 只有 Compose 静态校验，没有启动生产容器。

## 1.0.11 导航与设置安装（2026-09-24）

- 设置专项17/17、最后关于/法律专项3/3；导航专项65/65、最后TabBar专项2/2。统一source-mapped TypeScript检查exit0，diff check通过。
- 首次构建缺失依赖自带libsodium预编译文件，执行其原postinstall恢复后，重试自然exit0（7分44秒）。没有修改加密逻辑。APK为1.0.11/code12，原包名与签名不变；SHA256 `a8ce1042eaf4178a68e4a1f9d1c27e22151f18ede27884446297af77f805ee61`。
- 小米18799a47覆盖安装返回Success，dumpsys回读1.0.11/code12。未清除数据；手机前台在使用其他App，本批未切走当前画面，实际导航、设置布局、新建入口仍待真机验收。
- APK已归档到独立Happier仓库忽略目录 `.local/apk-archive/CodexTop-1.0.11-code12.apk`。本批为UI改版，不代表账号密码、真实推送或公网部署完成。

## 小米 15 安装回读（2026-09-24）

ADB设备18799a47识别成功；归档CodexTop-1.0.10-code11.apk经install -r返回Success，dumpsys确认versionName1.0.10/versionCode11，MainActivity启动Status ok、COLD。本机3009/ready连接失败（exit7），服务恢复及真机登录/会话/推送仍待验；安装不等于链路通过。

## 1.0.10 登录恢复及输入区（2026-09-23）

- 最后扫码提示RED1→GREEN5、独立正负2项通过；最终source-mapped noEmit与diff check通过，所有写权交回。
- 构建1分23秒exit0，原签名/包名、1.0.10/code11核验，模拟器覆盖安装与版本回读成功。SHA256 `d84d5a7766a6ea3d60fe638a397c3ac5887dd7455a12c54f5d28f6b50b7eec2b`，归档 `.local/apk-archive/CodexTop-1.0.10-code11.apk`。
- 原会话FOOTER107_OK、输入框与已完成均回读；截图确认模型/MCP入口消失、正常说明紧凑。证据 `/tmp/codextop-110-open.xml`、`-open.png`。独立文件链接@chip仍存在，实际仅插入路径文字，与autocomplete门禁不同，后续精简归原UI任务。
- user10签出后经带当前serverId的合成目标链接→Login→扫码页→密钥恢复，正常粘贴其测试账号自己的设备剪贴板密钥，恢复后到故意不存在目标的Session unavailable页而非首页。证据 `/tmp/codextop-auth110-{welcome,options,scan,manual,result}.xml` 及 `-result.png`，未输出密钥；manual采样在输入前，result已离开密钥页。
- 恢复Owner0，`/tmp/codextop-110-owner-restored.xml`确认原回复/输入框/已完成。仅证明当前来源手动恢复，不代表扫码配对/OAuth、多来源、推送或小米通过。CLI及PVTC未变，小米仍未连接。

## 登录恢复旧版实际基线（2026-09-23，1.0.9）

为保留原账号，主管在模拟器创建独立Android user10（CodexTop Auth QA），安装既有包；Owner0数据未清除。签出状态通过当前来源合成链接 `codextop-login-route-probe` 冷启，进入欢迎页，Get started后在127.0.0.1:3009创建独立测试账号，关闭通知邀请后落普通Sessions首页，没有进入目标会话的不可用页。证据 `/tmp/codextop-auth109-{baseline,options,created,result}.xml`。这是新账号入口丢目标的实际反例，不代替扫码或密钥恢复验收；账号密钥未读取或输出。结束后切回Owner0，user10留作后续独立验收。

## 1.0.9 通知导航与安装回归（2026-09-23）

- 两文件源码收口：已保存open_only复用canonical导航与显式serverId，不等待切服、也不写跨账号pending。RED挂起/拒绝两项失败，GREEN42项及source-mapped noEmit通过；独立通知入口/route scope31项通过。日志 `/tmp/notification-saved-{red,green,typecheck,independent-review}.log`，主管已核对。
- APK1.0.9/code10，原包名/签名保持；SHA256 `1d9c6e1d35adaa572474a7b97a4c06cfe0eaf3fb3f26547461db79c9ca61ac63`。构建1分22秒exit0，模拟器install Success并回读版本。归档 `.local/apk-archive/CodexTop-1.0.9-code10.apk`。
- 安装后当前来源链接打开指定FOOTER107_OK、输入框和返回入口，经典视图保留；该次Android报告WARM，不称冷启。断开3009、force-stop后链接报告COLD，显示明确离线及返回/重试；恢复3009不重导航即回正文/输入框。证据 `/tmp/codextop-109-cold.xml`（文件名cold但实际WARM）、`-offline.xml`、`-restored.xml`、`-restored.png`，截图已查看。
- 不代表系统通知回调/远程送达、多服务器或小米真机通过。小米未连接。未登录目标恢复已定位auth gate与各恢复入口，尚未修复；不能取消账号隔离来绕过。

## 当前来源链接与通知恢复缺口（2026-09-23）

canonical导航首批源码两文件已交回：29通知入口+6会话导航+6解析共41项通过，source-mapped noEmit通过。主管读过 `/tmp/notification-canonical-current-batch.diff`；该文件是精确编辑还原的批次差异，不冒称开工前文件快照。尚未构建/安装，因此系统通知返回栈未做本版实测。下一批同两文件改为已保存open_only导航先行以保留失败目标，授权实施中。

主管独立复跑真实pending复现5项通过，日志 `/tmp/codextop-notification-pending-audit.log`。已保存但无凭据的目标仍可能在refreshAuth后由认证redirect跳首页，登录后自动恢复未证明；不以有凭据路径替代该项。

模拟器1.0.8经现有scheme打开隔离账号唯一linked Happier session，运行中与force-stop冷启均出现指定FOOTER107_OK和输入框。缺失ID显示“会话不可用”；系统返回退出到launcher，再正常启动回首页。私有证据 `/tmp/codextop-link-{known,cold,missing,missing-back,return}.xml`。本次未传显式serverId，仅证明当前已保存来源链接，不是系统通知点击或多服务器验收。

真实pending owner临时合成测试5项通过，包含活动A保存目标B被拒、B账号激活后读取为空、同B控制组保存成功。账号隔离条件应保留；通知runtime切换前保存B与该owner契约不匹配，失败恢复及未保存服务器确认后的会话恢复仍不成立。open_only裸push另绕过canonical singular/显式来源导航，已授权两文件最小修复，尚未完成或进入APK。

## 经典视图现有偏好验收（2026-09-23，APK 1.0.8不变）

主管在隔离账号原会话菜单选择“打开经典视图”，原工作台底栏的文件/Git/导航/标签页均不再出现；指定合成回复与输入框、返回入口保留。返回首页能看到原短标题，force-stop冷启后再开同一会话仍符合上述条件。证据为私有 `/tmp/codextop-classic-open.xml`、`-back.xml`、`-cold-home.xml`、`-cold-open.xml`。这是已有账号偏好实际生效，不是全产品默认UI改版，也未证明其他设备同步。小米未连接。

通知只读审查确认现有 open_only 载荷沿 serverUrl + Happier sessionId 切换已保存服务器并进入原会话页；machineId不决定执行对象，实际电脑仍由会话元数据解析。ADB VIEW直接链接绕过通知点击解析与Expo回调，不能作为通知送达/点击验收。自有EAS/Firebase与系统通知实际路径仍待补齐，本阶段未发真实推送。

更新：2026-09-23。本记录对应复用 Happier 的新实现；旧自研安卓验收不能替代本记录。

## 已关联 Codex 来源名称同步（APK仍1.0.8）

- 仅既有LINK owner及测试2文件；明确Codex provider/机器/remote session/source相同，且用户点击候选传入新标题时刷新来源name。手动summary仍优先且原样保留，相同或缺失hint不写；原session复用，imported persisted不覆盖。
- 初版全provider规则使Claude既有集成失败，已限定Codex，未更改原保护断言。最终19单测、7集成及CLI typecheck通过。`/tmp/happier-link-title-{red,green-final,integration-final,typecheck-final}.log`。
- 官方CLI构建43.4秒exit0，fingerprint `afef073beb26bffd`，builtAt `2026-09-23T12:37:36.081Z`；仅隔离daemon官方重启PID95811，APK未重装。
- 实际首页旧正文名在打开前存在。浏览并选择原测试任务后，已知测试正文及输入框仍正常；返回首页显示短标题恰一条、旧摘要消失。force-stop冷启动后保持。证据 `/tmp/codextop-link-title-real-home-before.xml`、`-opened.xml`、`-home-after.xml`、`-cold.xml`。
- 会话页面header显示项目路径，因此不把它没有显示任务标题误判为名称同步失败。未实际测试用户手动改名并发，相关保护属于合成验证范围；也不扩大为所有已有会话自动迁移。

## 1.0.8 离线恢复与索引标题

- APK包名与原签名不变，1.0.8/code9；SHA-256 `5a9aa8bbecdf4a3b8752f88adb95bc17b88b62261ea1b536cade8ead68f5bd77`。构建1分30秒exit0，模拟器覆盖安装Success、版本回读正确。小米未连接，未安装本版。
- 离线修复仅route/SessionView及各自测试4文件，RED两项→最终30项通过；沿此前source-mapped配置noEmit零诊断。临时非映射检查的5处类型错误不作为产品新回归结论。
- 实际关闭3009反向连接并冷启动，打开已有会话后显示“未连接到服务器”及重试/返回；点返回回首页，重开后点重试仍保留离线恢复界面；恢复3009后同页自动显示既有指定测试回复和输入框，离线提示消失。私有证据 `/tmp/codextop-108-offline.xml`、`-offline-back.xml`、`-offline-retry.xml`、`-offline.png`、`-restored.xml`。18765反向端口保留。
- 索引标题5个CLI文件：每请求每home一次有界只读，以明确索引名称增强候选，不扩接口、预算或存储；61专项和CLI typecheck通过。官方构建48.54秒exit0，fingerprint `316504021d37d27d`，builtAt `2026-09-23T12:31:23.077Z`；仅隔离daemon官方重启PID93922。
- 手机真实浏览首50项耗时1089ms，指定测试任务、主管与手机UI显示正确短标题；点击测试项进入原会话正文。证据 `/tmp/codextop-108-titles-settled.xml`、`-title-open.xml`。已关联会话页仍用旧摘要名称，正在定位；不能据此宣称全App标题同步完成。
- 公网/多电脑/真实通知送达/小米及桌面原生新会话创建仍未完成。

## 1.0.7 后续 CLI 长行读取优化

- 仅原共用 `jsonlForwardReader` 和既有测试改为分片保留/新块扫描。既有 strictRead、完整行、oversize 和游标语义保持。资源RED、最终28项测试、官方CLI typecheck通过，104页合成差分一致。
- 成对基准近8MiB完整行原2028/1631ms、新56/51ms；实际读取字节和128次read相同。日志 `/tmp/happier-reader-{red,final-tests,typecheck-final,benchmark,equivalence}.log`。不是截断长行换速度。
- 官方CLI构建55.7秒exit0，fingerprint `156a0705dfb9f90d`；隔离daemon官方restart成功，PID80803。APK仍1.0.7，无需为CLI修复重装。
- 模拟器实际LIST第一页1138ms、第二页1550ms各返回50项。此前第二页21686ms；由于当前排序/文件活动可能变化，不按严格同数据对照宣称固定加速倍数。正确标题仍受appServer 750ms超时限制，另行定位。
- 离线冷启发现缺陷：打开缓存摘要对应会话后仅全屏spinner，没有离线文案和返回标题栏；网络恢复后同页正文与输入框回来。私有 `/tmp/codextop-107-offline-settled.{xml,png}` 与 `/tmp/codextop-107-reconnected.xml`；修复实施中，不能将恢复成功等同整个断线体验通过。

## 1.0.7 页尾恢复与分页实际操作

- 版本1.0.7/code8，APK SHA-256 `617a6959774d7a8c0e586b732273e8da32f0325101ef81bedfc802dd1b63705c`，原包名和签名；构建exit0、模拟器覆盖安装和版本回读通过。小米不在ADB列表，未安装本版。
- 页尾补丁仅列表组件与既有屏幕测试，复用原刷新动作和文案；25项专项与全UI noEmit通过。CLI继续既有产物，不重建/重启。
- 实际加载下一页两次返回各50项，后台耗时21686ms和1010ms；较慢请求最终成功，不是永久挂起。此证据不代表完整1040项已加载。
- 后续列表变化触发失效游标：旧列表仍在，页尾出现“会话列表已更新，请刷新后继续加载。”与“刷新”。因提示增高，需在页尾短滑约一行后按钮完整可见，不再反向滑多屏回顶部。点击页尾刷新后提示消失、加载更多入口恢复。证据为私有 `/tmp/codextop-107-next.xml`、`/tmp/codextop-107-footer-visible.{xml,png}`、`/tmp/codextop-107-footer-refreshed.xml`。
- 未宣称跨页全量完整性、真实推送、多电脑或小米通过。长行读取可能造成性能放大，已转有界合成数据验证，尚未证明其为本次延迟根因。

## 1.0.6 通知关注修复

- 原包名 `com.butang.codextop.debug`，版本 `1.0.6 / 7`，原签名核对通过；APK SHA-256 `dadb209cb5109a27d2301741818f17ef8cab3d905dd0916fa429d561b660694a`。Gradle构建1分22秒exit0，模拟器覆盖安装Success，安装后版本回读正确。
- 仅手机runtime及对应测试修复：租约身份排除关注/通知检查点回声，取消旧操作同步清busy。最终137项和全UI noEmit通过；CLI继续 fingerprint `9ec8c722b9ae1132`、daemon未重启。
- 实际菜单回读原关注已开启；取消后显示“关注任务通知”，再次开启后显示“取消任务通知”，未持续加载。force-stop冷启动重开后仍显示取消入口；最后取消测试关注并回读成功。私有XML证据 `/tmp/codextop-106-menu-{initial,disabled,reenabled}.xml`、`/tmp/codextop-106-cold-follow.xml`、`/tmp/codextop-106-cleanup.xml`。
- 同一daemon日志20:00:48–20:03:48只读窗口：ATTACH 10/10返回、DETACH 0；新页面后续约15秒续租，FOLLOW两次29/44ms返回，STATUS 75/75、READ_AFTER 73/73返回。没有此前metadata回声导致的连接反复释放；不把这段时间窗扩大为所有网络条件通过。
- 小米仍未在线，尚未安装本版；真实推送送达、跨网、多电脑及完整状态验收仍未完成。
- 动态分页实际验证：浏览首批并滚到“加载更多会话”，更新指定测试会话改变排序后再加载；旧可见行保留，顶部显示列表更新需刷新，点击刷新后提示消失。证明失效游标拒绝与刷新路径，不证明页外条目跨页时全库完整性。页尾缺少就地提示的问题已另行修补源码，1.0.6尚未包含该修补；证据 `/tmp/codextop-106-page-change-{settled,top}.xml` 与 `/tmp/codextop-106-refresh.xml`。

## 1.0.5 构建与安装（行为待验）

- 包名 `com.butang.codextop.debug`，版本 `1.0.5 / 6`，最低 API 24、目标 API 36；原签名 SHA-256 `ef3c64cbca2b79337d1e954474edf60667dc193a6ee156135723c3724a14f93b` 核对一致。
- APK SHA-256 `3c0f93e5eceb60d56d9bd2d5c3fdd4e67af62987b6277753be9166c9136f9e66`。包含历史可用性、动态分页及 viewer 生命周期/慢正文解耦的手机改动。
- 增量 prebuild、workspace 准备通过；首次 Gradle 因 Sentry 上传缺登录凭据失败，显式禁用本地自动上传后重试20秒成功。日志 `/tmp/codextop-105-assemble-retry.log`。
- 模拟器覆盖安装返回 Success，版本回读为1.0.5/code6，MainActivity启动成功；具体行为待配套 CLI 修复构建后验证。小米仍未在线，未安装本版。
- 配套CLI官方构建exit0，fingerprint `9ec8c722b9ae1132`，builtAt `2026-09-23T11:49:43.156Z`；主管已通过官方restart切换隔离测试home，PID71786、认证/机器注册/控制可达/机器ID及版本匹配均正常。Codex Desktop未重启。
- 模拟器实际初验：首页原 Logo、中文、既有连接和关联会话保留；设置显示1.0.5，更多设置可展开并进入外观。外观截图清晰，语言回读中文(简体)，玻璃模糊仍开启。未验证本版切换语言/模糊的全部路径；私有证据 `/tmp/codextop-105-{start,settings,appearance,glass}.*`。
- force-stop 后冷启动首页和浏览入口仍为中文，未返回登录页，证据 `/tmp/codextop-105-cold.xml`。
- 实际同桌面会话状态：App输入并逐字回读 `Wait 30 seconds using clock.sleep. Reply STATE105_OK. No file changes.` 后发送；原Desktop新轮运行38.454秒。发送后返回列表再打开该会话，等待工具仍执行时原生截图/XML显示蓝色“运行中”；完成后显示“已完成”，固定回复恰一条。私有证据 `/tmp/codextop-105-running-reopen.{xml,png}` 与 `/tmp/codextop-105-completed.xml`。没有接管或新建runner，不将这个单场景扩大为六态/锁屏通知/跨网通过。
- 通知关注未通过：点击菜单“关注任务通知”后重开，持续显示“加载中...”，后续采样仍不结束；未取得取消通知入口，关注是否持久化尚待只读定位。证据 `/tmp/codextop-105-follow-{menu,settled,later}.xml` 与 `-menu.png`。此前单元/集成测试不替代该实际失败。

## 1.0.4 构建与安装（行为待验）

- assembleRelease 自然退出 0，用时 1 分 27 秒；1995 项任务，90 执行、1905 复用。版本 1.0.4/code5，原包名和签名保持。
- APK SHA256：`93162abda6641b0199efbe68dd317cc8931142720c70698c9c507c7f9e4d2e0b`；签名 SHA256：`ef3c64cbca2b79337d1e954474edf60667dc193a6ee156135723c3724a14f93b`。
- 模拟器覆盖安装 `install -r` 返回 Success。该包包含语言持久化与通知 UI 最终修复，行为仍按下表实际验证；小米尚未连接、未安装本版。
- 语言实际复验：在 1.0.4 设置页选择简体中文并确认重启，首页中文；force-stop 后冷启动仍中文，外观页明确回读 `中文(简体)`。随后在真实 UI 将“玻璃模糊” false→true，开关回读 true、页面仍中文；再次 force-stop/冷启动后首页截图仍中文且正文清晰。该路径未复现 1.0.3 的语言回退。自动语言及小米仍未实测，模拟器暂留简体中文、玻璃模糊开启。
- CLI 官方构建退出 0，manifest fingerprint `a577818fe9b35a17`，版本 0.2.13；当前隔离 daemon 尚未切到该产物。认证状态回读 authenticated/machineRegistered/daemonRunning 均 true，不证明新通知逻辑已运行。
- 随后主管用独立 `mobile-emulator-cli` home、原 loopback relay 执行官方 `daemon restart`，exit0；PID由96765变为26926，状态回读运行、控制可达、机器匹配、认证均true。没有停止会话、没有重启 Codex Desktop 或修改 PVTC。
- 1.0.4 实际关注流程：指定既有测试会话的菜单显示“关注任务通知”；点击后重新打开菜单为“取消任务通知”。force-stop/冷启动、重新打开该会话后仍为取消；点击取消再打开菜单恢复“关注任务通知”。测试结束留为未关注。此证据证明手机操作与持久状态回读，不证明推送送达；会话状态显示“已完成”，其他五态仍待实际验证。
- 1.0.4 断线/恢复：仅移除模拟器 tcp:3009 reverse（保留18765），移除后现有页面最初仍可见旧状态，因此不据此断言连接已断。force-stop/冷启动后打开原会话，明确显示未连接服务器，无发送输入框。恢复3009后保持同一页面、未切后台或重开；10秒观察等待后重新采样，会话操作与输入框已自动恢复。只证明本次窗口内前台恢复，不推断精确恢复耗时或所有网络条件。
- 1.0.4 新 daemon 收发：先确认既有测试任务空闲，从 App 输入并发送固定合成请求（仅回复 `CODEXTOP_104_OK`、禁止工具及文件修改）。原桌面任务出现新轮 `01a0cded-2e01-72b1-8f1d-4e54daad958b`，6.531秒后完成；App 实际回显用户消息和确切回复各一条，未使用接管或新runner。发送时已开启关注，结束后点击取消；无真实推送送达证据。短任务采样未捕获运行中，保留该验收缺口，不以已完成显示代替全过程状态验证。
- **运行中状态失败**：后续通过同一App输入要求等待30秒的合成请求；Desktop新轮 `01a0cdef-648d-7c52-9797-c7b742b2e81d` 明确运行，App已回显等待说明和Sleep工具旋转，连续4次原生采样（19:03:39/44/50/53）及截图底部仍显示绿色“已完成”。当前1.0.4不能通过运行态验收，已交原云端任务只读定位。源码六态测试通过不能替代本项真实证据。

## 1.0.3 早前构建与初验

- Android 1.0.3 / versionCode 4，原包名与签名保持。Gradle assembleRelease 退出 0，用时 2 分 53 秒。
- SHA-256：`f9961b24ca810367d29dcb308b096422d83df60f5bf82c5bb17a3dff28460022`。
- 模拟器覆盖安装返回 Success，版本回读 1.0.3 / 4；实际启动保留账号连接与已有会话，首页及设置页原 Logo 可见，设置版本显示正确。
- 模拟器已通过UI将模糊开启并回读true；外观页面、返回设置再进入外观、随后浏览来源列表均实际可见，旧空白/泛白在这些路径未复现。发现切模糊后语言从简体中文回退自动英语，已交UI只读定位。“更多设置”已实际展开显示原通用/AI等入口，并可收起；聊天页底部不再显示接管按钮，更多菜单可见两种原接管动作，未执行接管。定位到1.0.2曾暴露内部引用的同一条历史消息，1.0.3正文完整显示且引用块不再可见；尚未实测复制和流式场景。
- 通知 UI 尚未接入此包，现运行 daemon 仍是早前构建；该包不能作为通知链路完成证明。小米未连接，未安装本版到小米。

### 下一包验收顺序（待执行）

| 场景 | 必须取得的实际证据 |
|---|---|
| 覆盖安装 | 原包名/签名、递增版本回读，账号和已有会话保留；分别记录模拟器与小米 |
| 中文保存 | 选择简体中文、确认重启、冷启动、切换模糊选项、再次冷启动后仍为中文；自动语言单独验证 |
| 通知关注 | 原聊天更多菜单开启/关闭，服务端回读一致；重开后保持；能力缺失或离线不显示已成功开启 |
| 显式状态 | 使用主管指定测试会话，运行/完成/需输入/失败/取消/未知各按明确事实显示，断线不沿用运行 |
| 未关注会话持续观察 | 不开启通知，打开测试会话后发送等待请求，整个等待期不得显示旧轮已完成；同一轮明确结束后显示完成。记录实际轮次与页面采样时间 |
| 查看连接生命周期 | 返回列表、切会话、切服务器、后台及重新进入后，旧响应不能覆盖新目标；旧查看连接释放或到期，新页面能重新连接；开启/取消关注不切断仍打开的页面 |
| 冷启动状态可信度 | 分别进入静止已完成和正在运行的测试会话；首份旧快照不得当作当前状态。若只能显示未知，明确记录未完成能力，不能以随后新轮正确代替此项 |
| 动态历史列表 | 首页后更新页外测试会话，使排序跨页变化，再加载更多；已有行保留并提示刷新。刷新后重新遍历，验证无因旧游标产生的漏条或重复 |
| 历史可用性 | 使用独立合成来源验证可读、仅预览、不可读及恢复；提示与真实来源一致，失败不清已加载正文或推进游标，恢复后重新读取当前来源 |
| 连续通知跳转 | 首次冷启动恢复目标后，再点击第二条通知，仍能进入对应会话；不出现未授权审批按钮 |
| 多服务器归属 | 目标服务器切换成功才进入目标会话；失败、未知服务器和旧异步结果不能打开当前服务器同 ID 会话 |
| 持续连接恢复 | 隔离边界测试证明进度保存失败后同一 lease 自动重试，下一轮可处理，原未知推送不重复 |
| 真实推送 | 使用自有推送配置和指定测试会话，验证锁屏送达、点击正确会话；Expo 接收回执不替代设备送达 |

上述表格是验收安排，不是通过记录。1.0.4 已验路径见本页对应版本记录；下一包等待动态分页与持续状态观察的配套改动交回，不提前启动共享构建。

## 1.0.2 早前验收基线

- Happier 基线 `ce5517b07e5b9ae6b340c9d6f7aa895758147bd8` 加本轮未提交适配。
- Android 包名 `com.butang.codextop.debug`，版本 1.0.2 / versionCode 3；仅 arm64-v8a，内嵌 Hermes bundle，关闭 OTA。
- APK SHA-256：`7029f80e9be939b33742e6d18fda0540ba2f5fbcd4d0aa6fba32539f232fcdfa`。签名与原安装包一致。
- 官方独立 sandbox 服务只监听本机，App 经 USB reverse 连接；这是本机验收，不是阿里云部署。
- 手机、模拟器及桌面操作均由主管执行。测试账号、CLI home 与凭据隔离；未修改 Codex 数据库、配置或认证，未操作 PVTC。

## 实际结果

| 项目 | 当前证据与结论 |
|---|---|
| 小米覆盖安装 | `adb install -r` 退出 0，返回 Success。随后 USB 断开；安装后版本回读、启动及配对尚未验证。未卸载或清数据。 |
| 模拟器启动 | 同一 APK 回读 1.0.2 / 3，真实 Activity 启动；首次使用、账号创建和通知权限页面可操作。权限允许不代表推送送达。 |
| 账号与电脑配对 | 使用官方手机授权流程，App 显示终端连接成功；官方 CLI `auth status` 回读 authenticated、machineRegistered、daemonRunning 均为 true。 |
| 已有会话 | 选择在线 Mac、Codex 和本机来源，按原生任务 ID 搜索并打开指定空闲历史任务，实际显示历史正文。尚未覆盖全部分页与大历史边界。 |
| 原桌面会话收发 | App 输入固定合成消息，只要求回复 `CODEXTOP_MOBILE_OK`，禁止工具和文件修改。原 Desktop 任务出现新轮次并完成，App 显示固定回复。未点击接管，未另启 provider runner，未重启 Desktop。 |
| 重开回显 | App force-stop 后重开，关联会话保留；打开后可见的测试用户消息和固定回复各一条。此项不替代全部分页去重验证。 |
| 断线提示 | 仅移除模拟器 3009 转发并重开，连接点灰色，进入聊天显示未连接到服务器，无发送输入框。没有提交离线消息。 |
| 恢复连接 | 恢复转发后页面起初仍离线；切到后台再回前台后原会话及回复恢复。未证明持续前台的及时自动重连。 |
| 简体中文 | 在模拟器通过语言设置选择简体中文并确认重启，会话首页、聊天提示与输入框实际中文可见，连接和关联会话保留；自动语言匹配与所有页面翻译尚未验证。 |
| 搜索与恢复 | 原生输入完整已知会话 ID，输入字段逐字回读一致，命中一条候选且转圈结束；清空字段后恢复多个来源候选。未证明标题/目录搜索、迟到请求隔离或搜索预算提示的新修复。 |
| 会话列表分页 | 清空搜索，滑到首批列表底部并点击“加载更多会话”；按钮显示 busy 数十秒后出现后续候选。继续滚动时原页末条与其后的 12 条新候选同时可见，视口内无重复。证明原生加载更多按钮链和可见页边界，不证明动态重排时全库无漏条，也未量化性能阈值。 |
| 历史滚动 | 连续手势可查看更早消息，固定测试回复离开视口；点击“跳到底部”后固定测试消息和回复各显示一条，按钮隐藏。尚未证明跨数据分页边界。发现内部引用标记原文显示，已交 UI 原任务登记。 |
| Android 空白页 | 模糊开启时外观及历史浏览子页发生空白/持续泛白，控件树已有内容；关闭玻璃模糊后恢复。已实现 Android 共用材质兼容修复，17 项专项与类型检查通过，尚未打入上述 APK。当前模拟器关闭模糊只用于继续联调。 |

原始截图包含私人历史与路径，仅在本机私有诊断位置使用，不提交。上述测试没有证明小米或公网结果。

## 已交回、待新版集成的修复

- 语言保存：最终 4 文件 41 项专项通过，最终语言改动后的全 UI noEmit 退出 0；语言页面、sync 保存边界及 focused 测试写权已交回。主管阅读了实现与失败/冷启动/旧服务端值/换账户测试；尚未新 APK 实际复验。
- 通知界面：首批 9 文件 186 项及 noEmit 通过，关注菜单、六态、open_only 路由与连续通知监听已实现；同服务器不同 linked session 迟到跳转缺口已由主管审查提出并由 UI owner 确认，限定两文件继续补修，尚未最终冻结。

2026-09-23 通知源码复核：云端任务最终报告 7 文件 105 项通过（通知边界 19 项）、源码 noEmit 与 diff 检查通过；主管已阅读关键生产路径和新增回归。claim 跨轮保留，只有相同轮次的明确 rollout 终态及读取游标一起提交后才允许回收；旧读取者不能倒退游标；CAS 回执丢失后保留本次尝试候选，并用实际保存的 claimId 验证发送归属。覆盖已提交/未知结果重放、并发 CAS 新候选、混合 Desktop/rollout 和 ACK 丢失。此为源码证据，旧 CLI dist 不含修复。持续连接下游标保存瞬时失败后的自动恢复仍待实现/验证，尚未切换 daemon 或验证真实推送。

- 品牌/手机设置：原 Logo 字节与主项目一致，应用内共享组件与 Android 图标/明暗启动图引用已统一；手机首屏为账号与连接、通知、关于，其余页内展开。专项 37 项通过；1.0.3 模拟器已目视验证应用内 Logo、设置版本及更多设置展开/收起，详见本页首节；小米和启动画面验收仍未完成。
- 浏览搜索：搜索不完整提示、保留快搜结果、清空后重置，以及同来源追加去重已交回，主管已核对生产差异和覆盖这些行为的测试；browse 7 文件 34 项通过，全 UI TypeScript 检查通过。未解决动态 offset 分页漏条。

## 未完成门槛

- 修复版 APK：恢复模糊偏好为开启后，验收外观、浏览、聊天、返回和后台恢复；再完成小米安装后核对。
- UI：原 Logo 的应用内覆盖、紧凑手机设置、中文覆盖与内部引用标记显示修复，以及直接发送与显式接管入口层级。
- 历史完整性提示：缺失/不可读原始记录当前可能被当成空历史或普通预览，尚未区分。已选定添加可选可用性事实（available/preview_only/unavailable，缺省 unknown），沿现有分页数据链传播；等待通知任务交还共享协议写权再实施。动态列表分页先采用变化检测后要求刷新，不用去重冒充无漏条。
- 实时状态：当前 Desktop 工作时 App 仍可能显示 Inactive；必须接入明确状态，不用发送回执猜运行。
- 通知：自定义包缺自有 Firebase/EAS 推送配置；Direct 显式完成/需输入/失败、持久关注、防重与发送器接入正在实施。真实锁屏送达、冷启动点击正确会话均未验证。
- 项目与创建：现有普通新建启动独立 Happier runner，不能算 Desktop 拥有的新会话；已确认 Desktop 自身受授权工具能创建项目会话，但普通 Happier 进程调用该管道被签名授权拒绝；现有 follower IPC 无创建入口，未尝试绕过。已询问用户是否接受明确标识的独立新建，未答前不替换原目标。
- 多电脑、公网 HTTPS、阿里云隔离部署、重启恢复和跨网验收尚未完成。

依据入口：[REQ-002](../REQ-002-局域网多设备监控/requirement.md)、[主管记录](../REQ-002-局域网多设备监控/supervision.md)、[当前状态](../STATUS.md)。
