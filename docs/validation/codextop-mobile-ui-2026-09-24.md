# Codex Top Mobile 中文与界面样板验证

2026-09-24。对应 REQ-002 D-06/D-07。代码已完成本批专项，不代表正式 Android 界面或端到端业务验收。

## D-08 正式改版与模拟器接续

### 1.0.17 双向消息与当前轮状态

1.0.17/code18 构建4分42秒成功，原签名验证通过，APK SHA256 `74d1bc3bb01959481fbc7f426001c9d890235242a28255296529f1b64627b3b3`。覆盖安装模拟器独立用户并回读版本成功；输入正常宽度、原草稿保留、聚焦及输入不再白屏，工作台五入口和手机VCS工具行不再出现。输入/提交专项42项、原生光标23项、经典手机路由68项、UI noEmit通过；光标RED曾复现同样的最大更新深度异常，不能将先前294项覆盖误写为已覆盖此原生问题。

实际双向检查：保持同一原Desktop测试任务和手机页面打开。电脑端发起30秒等待的合成任务后，手机自动显示运行中；完成时显示已完成并收到 `DESKTOP117_OK`。随后手机实际点击发送合成文本，原Desktop同一任务新增一轮，手机依次显示运行中、已完成和一条 `FUNC117_OK`，草稿清空；没有另开执行器。正文和状态各有真实原生界面证据，原始图/XML仅保留本机私有诊断目录。

仍有明确阻断：冷开静止历史时首次发送显示“暂不能发送补充”，没有发出消息。上述双向通过依赖持续订阅先观察到一轮，不等于冷开可用。已授权原UI/电脑接入任务修复：发送前沿现有控制读取验证同目标当前文本选路，保留首包生命周期未知保护和原单次投递回执；不依据缓存完成或列表状态放行，不新增RPC。计划新UI先安装，再更新隔离CLI，旧UI遇新strict快照可能安全失败，不能称双向兼容。修复与冷开复验尚未完成。

### 1.0.16 实际功能检查

2026-09-24：1.0.16/code17 正式路由构建成功，原包名和签名证书保持，APK SHA256 `3702c7ae66fad95b47e36dda37a2dca75f92665391e6da9d375eea0b3ad4826a`。覆盖安装模拟器独立测试用户并回读版本成功；用户0与小米未操作。A账号登录在升级后保持，首页实际显示精简三态、搜索图标和紧凑行，没有电脑/项目选择块。打开已核验的原Desktop测试任务可读历史，未另开执行器。

源码验证：A+B去重23文件294项和独立审查完成；C首页4文件106项、完整UI noEmit通过；CLI候选生命周期81项、协议及独立边界复核7项通过。协议与CLI官方构建成功，隔离测试连接组件采用官方 takeover 更新，无 kill-sessions，账号与机器身份、控制连接回读正常。列表生命周期是有界读取，无法覆盖明确主轮锚点时返回unknown，不能据此称全部历史状态已经完整。

实际阻断：原生对话输入框被挤到极窄；点击并输入后触发 `Maximum update depth exceeded`，React页面白屏。没有点击发送，不能记为消息已投递或本版收发通过。另发现新账号仍默认进入包含文件/Git等入口的工作台，正在收敛到已确认的手机对话职责。原手机UI任务修输入与键盘，独立审查定位真实堆栈；主代理统一操作设备，修复后须重新打包复验。

真实审批尚无受控待审批请求。本版接入支持命令/文件审批；原生用户选答请求尚不支持手机应答，须如实转电脑处理。目标策略若不触发人工审批，不修改其策略或制造伪请求凑验收。通知缺自有推送配置，深链路由不能替代系统通知送达/点击。

### 1.0.15 中文基线（历史记录）

1.0.15/code16 已构建、验签并覆盖安装模拟器独立用户，SHA256 `58be0ebd980476109e16d1f13343aac54e03095d487a882ee6243b300fceaec0`。首次构建因 libsodium 官方二进制未解包失败，运行该依赖自带 postinstall 后重试 exit0；没有改业务源码绕过。该包只有此前中文/品牌批次，实际仍是旧布局。

已实际验证：旧1.0.14设为英文且冷启动回读后，保留数据升级1.0.15仍中文；旧语言深链回到外观；账号退出、冷启动保持退出、正确账号重新登录及冷启动保持登录；系统阻止通知时中文提示。测试账号不关联真实电脑。系统本身是中文，不能称英文系统覆盖；全新模拟器用户首次通知邀请已实测显示中文与 Codex Top，点击“暂不”后冷启动未重复邀请。测试后恢复原测试用户；这不代表通知实际送达。

实测新发现：外观下拉仍有英文内置主题与只读预设提示；深色首页搜索占位文字对比不足。前者在A批收敛三项主题并保留旧值，后者交C首页；不以词库静态审计代替真实页面检查。

A+B正式页面27文件已交回：紧凑导航与原Logo、平面电脑/设置、三项主题、对话标题/消息/输入/审批展示，复用原业务和动画owner。去重23文件294项专项、完整UI源码 noEmit通过，A设置/导航独立审查未发现可确认P1/P2，B对话审查进行中。尚未进入APK，不作为视觉/键盘/动画验收通过。C首页与现有LIST生命周期事实接线正在并行实施，无新API、无全列表租约；后续构建后必须连续验证实际页面与恢复路径。

## 已完成

- 产品界面固定简体中文；历史英文偏好仍按原协议保存，但不影响显示。语言入口移除，旧深链返回外观设置；用户消息、代码、项目和任务名保持原文。
- 通知邀请、崩溃、登录/连接及获准权限、加密、版本错误使用中文和 Codex Top 名称。其他语言兼容资源保留，不继续扩展适配。
- Cursor 工作区显示名为 Codex Top Mobile；App 各环境默认显示名为 Codex Top，不依赖本机覆盖文件。物理源码目录暂保留，未中断现有服务路径。
- 隔离 RN/Paper 样板含精简会话、对话、电脑、我的，默认正式路由不变；显式预览开关与开发环境同时成立才启用。样例数据不连接真实账号、同步或 RPC。

## 验证证据

| 范围 | 结果 | 限制 |
|---|---|---|
| 中文与设置 | 13项新增失败复现后，24项通过；最终适用回归合计167项通过 | 手机 UI 任务执行，主管核对交接与日志；未安装新版 |
| 中文词库主流程 | 975条静态核对，缺键0、上游品牌残留0、动态名称原样保留 | 未开放上游功能和法律归属不冒充产品品牌清理范围 |
| 样板/配置 | 4文件46项通过；修正只读环境类型后的配置35项再次通过 | 空本机配置夹具隔离了开发者 app.local.js，不改变私有配置 |
| UI 类型检查 | 完整 UI 源码映射 noEmit exit0 | 未生成 APK |
| Paper | 5.15.3 安装、官方 postinstall exit0；React/RN/safe-area 同实例解析 | 仅使用所需组件，不更换业务链路 |
| Web 连续操作 | 393×852视口：运行中/待处理、中文搜索、进入/返回对话、拒绝示例审批、发送示例消息、底栏、浅/深色 | 全部合成数据；未验证软键盘、原生动画及减少动态效果 |
| CLI 来源/标题 | 修复后63项、CLI noEmit、独立复现复核通过 | 尚未打包替换运行中 daemon，不代表手机候选已更新 |

独立审查已修复两项：App Server 的 `source.subAgent/threadSource` 与 rollout 字段差异；已知注入标签必须精确匹配，不能截掉 `<app-context-menu>` 等普通用户文本。

## 未完成与下一步

- 将获准样板接回正式业务界面后，统一 APK 构建、安装及中文冷启动/通知弹窗实测。
- 来源候选的完整真实三态尚未接入，不能将 mtime 活跃度当作运行或完成；unknown 不放入已完成。
- 原生协议已有可选 Thread.projectId，需验证与保存项目 ID 的对应，再修复 worktree 项目归属；本批未猜配。
- 独立部署包已交回，[本地审查24项及两项修复复核](codextop-deploy-package-2026-09-24.md)通过；公网、HTTPS、自有推送、同事正式账号未交付。未部署或改动 PVTC。

本批本地中间日志与交接置于 companion 的忽略目录，不提交原始诊断、账号信息或任务正文。
