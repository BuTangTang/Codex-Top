# D-66：恢复两按钮初版尺寸

日期：2026-09-18。最终包：**1.0.0 build 10**。

用户通过对话截图指定恢复到开始讨论“按钮偏小、整体字号”之前的样式。定位到本任务 17:25 的 build 8 原始修改：已经只保留“＋ / ⋯”，尚未实施 D-65 的字号和图标放大。通过新提交恢复该外观，保留 Git 历史；没有采用随后生成的 A/B/C 设计图。

## 恢复范围

- `MonitorView` 与 `2be13e2` 加首次 D-64 修改的重建结果逐字节相同；`Appearance`、`UsageViews` 与该基线对应文件逐字节相同。
- 图标恢复原生 SF Symbols、14pt medium、28×30pt 区域、4pt 间距及 80% 主文字色。更多菜单恢复原生 borderlessButton，不再自绘放大的圆点。
- 标题、任务名、说明、数量、计时和额度恢复原字号、字重、颜色与可读下限，删除 D-65 的固定两级字体方法。
- “＋”选择任务；“⋯”里的显示方式、主题、设置和收起功能保留。TopPanelView、WindowController 的收起回调保留。数据与状态逻辑未回退。
- 构建号递增至 10，外观恢复不冒充重新安装旧包。真实关注列表和用户当前显示设置保留。

## 验证结果

| 项目 | 结果 |
|---|---|
| `swift test` | 122 项，0 失败 |
| `swift build -c release` | 通过 |
| 通用应用 | arm64 + x86_64 构建通过 |
| 严格签名 | dist 和已安装应用的 `codesign --verify --deep --strict` 均通过 |
| 安装一致性 | 已安装与 dist 可执行文件逐字节一致 |
| ZIP | 重新打包，`unzip -tq` 通过 |
| 源码恢复依据 | 三个外观文件与历史重建结果逐字节一致 |
| 真实应用 | 原圆环/深色/75%/5 条恢复，实际任务与计时可见，新旧入口功能正常 |
| 加号 | 浮窗打开任务选择器，取消仍保留四个合成关注任务 |
| 更多菜单 | 显示方式、主题、设置、对应关闭或收起项可访问 |
| 四模式 | 浮窗、圆环展开、内置刘海展开、状态栏弹层逐一核对并实拍 |
| 返回来源 | 关闭浮窗后返回圆环，再展开正常 |
| 主题 | 最终包浮窗浅色玻璃与深色可见，菜单切换通过 |
| 本轮比例 | 真实圆环 75%、合成浮窗 75%/100%，四模式实拍均为 100% |
| 外接屏 | **未复测**：设置和实时 NSScreen 枚举均只有内置 Retina 屏；不沿用 build 9 外接屏结果充作本包验收 |

可执行文件 SHA-256：`ce0ef289ce2c439cdf6badbd66fea0dcaf060914bbabdf234bc00b6a0023262c`。

本地 ZIP SHA-256：`0a1e6b950415e5c33fe2064dfaf937abd13d781ce3af12f956236dc86c037e16`。

## 截图与恢复

中英文 README 的四张图均来自已安装最终程序，使用独立状态目录与 `--demo` 合成任务。拍摄先退出真实实例，演示退出后再恢复真实实例，始终只有一个监控进程。

| 图片 | 像素 | 场景 |
|---|---|---|
| [floating-dark.jpg](../images/1.0.0-build10/floating-dark.jpg) | 360 × 210 | 内置屏常驻浮窗，100% |
| [orb-expanded-dark.jpg](../images/1.0.0-build10/orb-expanded-dark.jpg) | 410 × 290 | 内置屏圆环展开，100% |
| [notch-dark.jpg](../images/1.0.0-build10/notch-dark.jpg) | 410 × 322 | 内置刘海屏展开，100% |
| [menubar-dark.jpg](../images/1.0.0-build10/menubar-dark.jpg) | 410 × 290 | 内置屏状态栏展开，100% |

图片保存 Computer Use 原始 1× JPEG 字节，未放大或重绘。逐张目视只有合成任务、演示用量提示，无真实任务、账号、私人背景、完整本机路径及调试鼠标覆盖。元数据仅有尺寸、色彩配置与密度字段。

真实偏好文件在隔离拍摄前后逐字节一致；收尾恢复唯一的无 `--demo` 正式实例，实际圆环已显示真实运行状态。测试目录和旧包备份仅留在本地忽略目录，调试鼠标提示清理。历史 build 9 图与验收保留，README 当前展示 build 10。

本阶段源码、双语 README 与截图一起提交推送，不发布或替换 GitHub Release。待外接屏重新连接后补做本包外接场景；60%/120% 端点、连续动画/FPS、物理拔插合盖和 Intel 真机本轮未重新测试。
