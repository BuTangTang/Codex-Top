# R01–R19 验收对照

更新：2026-09-11。第一版目标是可构建、可运行的原生应用、测试、使用说明和真实验收记录。以下按原需求逐项审核，没有把正式 Release、公证或 CI 授权扩张成新的产品需求，也不把未完成的原需求删去。

当前没有“所有要求已完成”的结论。代码、合成测试、实际应用加合成数据、真实用户数据、物理设备操作分别记证据，不互相替代。

当前修订到 D-51：92 项测试、最终通用构建、应用签名和 DMG 只读挂载校验通过，桌面已更新；README 已放实际程序的合成任务截图。实际比例、计时和截图范围见 [beta.2](beta2.md)，不再等待解锁。

D-48 / beta.1 已有 78 项测试、universal、严格签名和公开预发布；其截图与完整拖动当时因锁屏未验，仅为历史边界。更早 D-47 的真实运行行与已结束项、独立额度暂停核对也不替代 beta.2 证据。逐次结果见[本轮记录](notch-orb-refinement.md)。D-40 透明底背景仍未实施，不将端点截图当成最终动画帧率验收。

| 要求 | 已有可核对证据 | 未证明或限制 |
|---|---|---|
| R01 本地非归档任务 | [M1](M1.md) 的 SQLite 字节不变/归档过滤测试、真实任务读取；[M3](M3.md) 实际应用显示本机活动任务 | 本地已落盘任务；不包括未同步的云任务 |
| R02 状态 | CoreTests 生命周期、7 种终止事件轮次隔离、失败保持、明确输入/批准、过期未知；实际状态变化；D-41/D-42 源码定位任务行运行弧原先固定角度，现接入原生持续旋转 | D-46 五项增量回归覆盖大输出/同步/提问保持；真实运行弧端点方向已变化，不代表测得FPS；尚未覆盖真实 Codex 所有批准事件变体 |
| R03 一份关注列表 | 选择规则测试；[M2](M2.md) 顶部/浮窗/圆环显示同一批选择；后续模式切换未改变关注 ID | 已有范围内未发现缺项 |
| R04 搜索和多选 | 项目搜索、全选当前结果、取消/确认已有实机记录；草稿合并测试 | 已有范围内未发现缺项 |
| R05 自动加入和排除 | 同秒基线、旧偏好和排除测试；[M3](M3.md) 新任务出现→加入→排除、关闭/重开、真实进程重启的合成数据链 | 数据库整秒创建时间存在文档列明的同秒歧义 |
| R06 首扫、父子和结束 | 首扫排除历史完成项、TaskGraph 测试；实际应用中子任务待回答提升主任务，子任务完成后主任务进入已结束 | 聚合链使用合成数据；所有实际子任务来源格式未穷举 |
| R07 显示器与恢复 | 已有坐标/找回、2 项按钮 resolver 和 D-39 的 3 项同宽安全区回归；既有外接屏与 D-34 初版真实刘海。D-49 已实现按各窗口实际 screen 选择字号，external 紧凑、builtin 常规、nil 保持，常驻强制紧凑 | D-49 的实际跨屏、外接设主屏、暂缺屏幕与常驻优先级待本轮核对；物理重排、拔插、合盖及跨屏组合不能由几何测试替代；D-40 仅讨论 |
| R08 置顶浮窗 | 实机浮窗拖动、关闭、重新展开、同列表 | 跨应用长期置顶完整场景尚未记录 |
| R09 返回任务 | UUID 路由测试、安装包及系统 handler 核对；D-32 修复后台回调主 actor 断言，后台实验与实际点击后存活已通过；D-37 最终真实包再次点击任务后收回且保持运行，见 [本轮](notch-orb-refinement.md) | 未读取或核对 Codex 对话终点；系统接受打开不代表对话读取成功 |
| R10 用量 | 10 项协议/超时/取消/目录与合成换号隔离测试；8 项摘要测试含实际周期、剩余/缺失/过期；D-38 实际包已看到账户额度；D-49 已将额度字号接入每窗口常规/紧凑策略 | 历史 80% 账户来源可见不证明 beta.2 各档字号；实际 A→B 换号、网页终点未验；每次显示仍需按来源和观察时间判断时效 |
| R11 设置 | 隔离目录选择、暂停/立即刷新、偏好与进程重启；登录项显示改为回到应用后读取系统状态 | 系统允许/拒绝开机启动及实际下次登录未操作验收 |
| R12 故障提示和恢复 | 缺失/不可读/不兼容核心测试；实际应用无数据库、暂时移走数据库、改坏表结构均显示提示并能恢复 | 已覆盖列明故障，不等于未来所有格式都兼容 |
| R13 性能与只读 | 只读连接、目录边界、数据库字节不变测试；有界尾部读取、无变更 0 字节、本机冷启动时间；额度超时/取消测试验证等待返回后的进程回收 | 本机读耗时不能证明所有机器性能或动画 FPS；D-43已增加等待自有请求清理的退出流程，极端实际退出时序仍须区别于合成测试 |
| R14 构建和说明 | beta.2：92 项测试、universal、严格签名与 DMG 校验通过；Windows 提示词和实际程序截图已同步 | ad-hoc 未公证、CI 未启用；镜像和源码回执见 beta.2 记录 |
| R15 比例 | 13 档实际控件值与显示百分比一致、100% 恢复，60% 头部/选择器/计时后缀、120% 展开、80% 双主题与常驻窗口可见；5项核心回归通过 | 原生连续拖动配置已实现，工具鼠标拖动未取得可靠位移证明；未逐档遍历全部物理屏/模式 |
| R16 圆环 | [旧记录](M2-circle.md) 的同窗口开合/拖动/右键；D-35/D-36 隔离示例端点；D-38 拖放 hover 修订已交付；D-41/D-42 同事务画布与窗口布局、最终内容尺寸、0.96 内层缩放已改代码，44pt 球默认视觉不变 | 用户仍反馈开头与列表抖动，新方向修复必须以最终包复核；不能声称抖动消失或 FPS 达标；D-38 顶部真实拖放、减少动态效果和物理屏组合仍未完整验收 |
| R17 自由拖动与状态栏 | 既有拖动/重启/圆环右键；D-32 80% 菜单栏面板、D-33 实时锚点、D-34 初版真实刘海、D-39 同宽；D-48 已取消所有拖放切模式并接通非按钮顶部拖动 | 历史 beta.1 完整顶部拖动因锁屏未验；用户现已解锁，需在 beta.2 核对标题/留白、按钮不误拖、平移后球锚点及顶部不切模式。系统图标实际左/右键、新比例、物理重排与系统明暗仍未完整验收 |
| R18 配色和过渡 | 既有径向主题与稳定端点；D-49 浅色 waiting 统一橙色 `(0.91, 0.43, 0.06)`，圆环和任务圆点同色，标签12% 淡橙底配黑色 0.82 文字；深色亮琥珀配 tint 底 0.12，文字底片圆角 4pt、留白 5/2pt | 浅深面板和淡橙底已在实际副本核对，未覆盖所有壁纸与物理屏；D-40 透明底仅建议未实施；全部状态、减少动态效果、完整中间帧与 FPS 未全面验收 |
| R19 待处理时长 | 9 项时间锚点、回复、新轮、多问题、缺时间和子任务回归；最终实际副本待处理固定 03:24，运行计时继续，60% 后缀可见 | 本轮开始到进入当前等待的经过时间，不是 CPU 时间；冷尾读缺时间不估算 |

## 剩余验收所需条件

- 已完成本轮窗口截图、原生控件数值、恢复 100% 与计时端点核对；完整鼠标拖动、所有比例与屏幕组合、全部动画中间帧仍待验。截图副本和唯一 CUA 提示服务已清理，保留真实应用。
- 可操作的物理屏幕与现场操作：内置刘海、拔插/合盖、重新连接及各比例。几何测试不能代替。
- 系统开机启动与减少动态效果需要实际系统设置流程；未自动接受权限、未替用户修改这些系统设置。
- 真实任务跳转终点受当前 CUA 对 Codex 应用的访问限制；不绕过。
- CI 授权、Developer ID、Apple 公证和正式 Release 属于分发/运维边界，分别记录，不能伪造已交付。
