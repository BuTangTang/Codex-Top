# Mac 设置双栏与正式连接（build 43）

2026-09-25。依据 REQ-002 D-11，用户要求左侧选择分类、右侧显示详情，避免通篇滚动。

## 已实现

- 左侧固定“显示与外观、任务监控、账号与连接、账户额度、数据与启动”，右侧仅显示当前分类并独立滚动。原设置控件及动作保留。
- 原生列表支持鼠标和方向键；约 140ms 淡入淡出，减少动画时关闭过渡。
- 默认窗口 820×650，最小内容尺寸 740×480。显示器列表通过可观察状态更新，重排不再替换设置视图，避免分类与账号输入丢失；离开账号页仍清理密码。
- 自用构建可注入默认服务地址，非空用户已保存地址优先。公开源码不内置实际部署地址，也不因默认地址而显示已登录。

## 版本与检查

- Mac 正式应用：1.0.0 build 43，已覆盖安装，签名检查通过。仅保留一个可见正式实例，无 Demo。
- 内置连接组件：1.0.0+codextop.43，指纹 `f2bf722d60740710`。
- 最终 Swift 203 项测试通过；最终 release 构建及连接组件正式构建通过。显示器视图保持修复另经独立 target 构建通过。
- 正式窗口实测：五分类内容可达，鼠标与方向键选择一致；深色、浅色排版无裁切；原圆环、105% 比例、任务监控和启动设置保留。
- 正式产品连接目录通过原组件 stdin 密码登录，身份回读确认与手机现有联调账号相同，机器注册及 daemon 启动成功；真实账号页显示账号、组件已启动、Codex 已打开。此项证明正式产品登录与状态回读，不冒称通过鼠标提交密码表单或新机器收发验收。

## 状态读取修复与限制

LIST 修复已随连接组件打包：尾窗能凭完整主轮 ID 和明确终止事件识别完成，不要求开始事件也落在同一 64 KiB 窗口；换轮、非法行、未闭合尾行及待处理冲突仍保持未知。同 inode 追加时最多重读一次，总预算由正常 128 KiB 限制到重试 256 KiB。98 项专项、CLI 类型检查通过，独立 8 项大文件复核通过；未把模拟样例当手机真实列表全量验收。

CONTROL 仍存在当前长任务读取超时：只读探针快速发现 owner，但 7 秒内无目标状态帧、无关联历史回执，未观察到明确的原生拒绝原因。不能据此确认是历史量或 owner 缓存导致，不延长产品发送超时，也不以任意快照伪造状态。真实审批、全部冷历史状态和新正式机器收发仍待验收。

用户在浅色验收后操作新版设置时曾停止点击。最终连接标识修复已重新安装，恢复原深色并在账号分类回读已登录／已启动；缩小窗口、减少动画和外接屏事件的真实验收尚未完成，外接屏当前未连接。

正式连接收口：修复原生服务标识73字符超出CLI64字符限制的问题，采用固定前缀加55位摘要，稳定性／合法字符／不同服务测试先失败再通过。将旧QA产品目录整体迁入正式位置，通过既有server set迁移同URL非密映射，并移动完整服务目录；原machineId、installation身份保持，官方status确认原账号、机器注册及唯一正式连接启动。原QA进程已停止，其目录已移走；本次误建的额外机器通过既有revoke入口撤销，服务器回读原机器在线、额外机器撤销，不删除会话或消息。迁移备份仅私有保留；Codex目录及PVTC未改。

LIST两文件提交dev `d886f9f0b`，自有发布快照 `023952828` 已推送且远端回读一致。初次推送使用失效代理失败，单次Git调用直连重试成功，未改全局代理或TLS验证。
