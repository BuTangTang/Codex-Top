# D-79 统一浅色实色外观

日期：2026-09-21。用户确认把现有浅色监控统一为右侧菜单的不透明浅白，保留“深色／浅色／系统”三项，不改尺寸、字号、布局和动画。

## 实现范围

- 背景共用 0.98 浅白，深色仍为黑色。移除监控及主题快照里的原生玻璃层、刘海和折叠圆环的旧补白；提醒／失败的状态着色保留。
- 浅色细边改为黑色 10% 透明度；原线宽、圆角和内缩保持。展开圆环也显示同款细边。
- 设置与菜单完整名称从“浅色玻璃”改为“浅色”。主题枚举、保存值和系统跟随解析不变，不添加新主题或材质开关。
- 主题快照的底色同步为实色。遮罩、运动、时长、延迟、取消逻辑、窗口控制器、列表与数据路径没有本轮变更。

## 验证

160 项测试零失败，`swift build -c release`、arm64/x86_64 通用包、严格签名、安装目录与 dist 完整比较、ZIP 完整性通过。

隐藏原生测试检查深浅背景的 alpha 为 1、浅色 RGB 约 0.98，两种主题及尺寸变化都不包含 NSVisualEffectView；准备和替换主题快照后，旧图保留且不会重建玻璃层。既有系统跟随、手动主题优先、菜单尺寸和动作测试通过。测试使用独立临时目录，不显示桌面 Demo。快照测试没有启动逐帧动画，不能等同于动画中断过程的连续画面验收。

真实 build 31 已安装并启动。用当前真实圆环打开监控，实际截图确认系统浅色呈不透明浅白，菜单同色且三项主题完整。通过菜单依次切换深色、手动浅色，均取得原生窗口截图，深色仍黑底，浅色没有桌面染色。复查时当前主题已变为深色，保留最新选择；最后关闭菜单并收回圆环。

安装时备份当前正式包，退出旧进程再更新，最终只有一份真实应用。验收期间用户改变了主题、屏幕和位置，本轮没有执行拖动或切屏，也没有用旧偏好文件覆盖最新状态。圆环模式、105%、5 条、3 天整理期限保留；Codex 数据未写入。原始截图和真实任务标题不提交，公开 Release 不变。

| 产物 | SHA-256 |
|---|---|
| build 31 通用程序，已安装且与 dist 一致 | `b277c3b0fbf97adef9a60143b55338a9b8e814805cec39f3adf1fd1c34fabd91` |
| build 31 ZIP | `58512a310c5e0f44d8e382c64983c3e8b56bd0d92ded2eb98fa30c471b355a62` |

## 边界

实际验收覆盖本轮当前窗口中的系统浅色、手动浅色和深色及菜单；没有重做四模式、指定显示器之间的受控对比、拖动或连续动画测量，不借用历史结果宣称这些已通过。外观改动之外的既有问题和功能不在本轮扩展。
