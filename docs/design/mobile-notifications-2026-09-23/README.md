# 手机通知页设计预览

## H 紧凑消息列表

用户继续指出占比过大，要求参考微信消息列表的大小。新增 [H 紧凑消息列表](h-compact-inbox.png)，一屏九条完整通知与部分第十条，连接状态并入顶栏，每条仅任务名称、状态/来源和时间。已检查文字与条数；这是内置 imagegen 生成的比例概念图，不能当作精确 dp/sp 或已实现的 APK。尚未选定。生成提示：不透明白底 Android 平面截图，393×873dp 概念视口，52dp 顶栏、76dp 两行列表、16sp 主文字、13sp 摘要、小状态图标、右侧时间，禁止手机外壳、反光、大标题、底部导航和多余功能。实际实现需重新核验图标尺寸与分隔线。

用户另提出未来从手机向服务器发送消息，再转发到目标电脑上的 Codex Top；直接发送到对应 Codex 任务还是电脑提醒尚待明确。本图只展示通知列表，不代表远程操作已授权或实现。

2026-09-23，用户明确只需要通知，认为原界面不好看，要求先出设计图。本阶段仅生成视觉概念，没有改 APK、桌面程序或服务器。

三套均为单页，包含连接状态、设置入口、按日期分组的通知。使用合成任务，无聊天、远程操作、任务管理和底部导航。

- [A 极简列表](a-minimal.png)：白底、细分隔线、圆形状态标识。
- [B 柔灰分组](b-grouped.png)：浅灰底，今天与昨天各一个白色分组面板。
- [C 深色时间线](c-timeline.png)：深灰底，以状态节点串联通知。

图片使用内置 imagegen 生成。已逐张检查中文、通知状态、设置及单页范围；均为设计效果图，手机边框、反光及状态栏是展示元素，不是待实现的页面装饰，也不代表已通过实际字体、无障碍或真机验收。用户尚未选定方向。

生成提示概要：ui-mockup；1024×1536 竖版 Android 单屏，中文 MiSans/Noto Sans CJK 风格；标题“通知”、状态“已连接”、右上设置；相同四条合成通知：登录页面调整需确认、通知功能测试已完成、安卓安装包构建失败、连接状态检查已完成。A 使用白底墨色正文和细线分隔；B 使用柔灰背景与两组白色面板；C 使用石墨深色背景和状态时间线。禁止底部标签、聊天框、图表、指标、头像和大按钮。保持一致内容以比较排版，不增加功能。

下一步由用户选择或指出需要调整的元素，再细化对应设计并实现。佳明仍不在当前范围，PVTC 隔离约束保持。

## 实施确认

用户在 H 预览后要求“开始实施，你自己跑通测试再说吧”，确认 Logo 沿用已有蓝环橙点，并要求“不需要的接口少做”。本次按紧凑通知页实施，服务登录、状态读取、退出三接口保持原样；Logo 复用 `Resources/AppIcon.png`，不从截图裁切或重新生成。真实安装与测试结果见[安卓验收](../../validation/android-mobile.md)。
