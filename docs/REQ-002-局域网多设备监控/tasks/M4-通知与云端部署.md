# M4 通知与云端部署

2026-09-24 最新进展：业务镜像已构建、真实容器验证并打包上传；此前“镜像构建中／脚本仅静态”的记录为历史过程。`codex-top-server:2026.09.24-1` 为 Linux amd64/Node22，SQLite、UID1000、stdin 开户、停启持久化及 HTTP 密码登录/解封/Bearer 身份闭环通过，独立测试资源已清理。首次60秒观察窗口在Docker健康采样前结束，同一测试状态续用90秒后完成，不重建数据库掩盖失败。镜像归档755MB，SHA-256 `428474a0c7c18a2893c921b12ea6478a26d0f2cbc8690088bc41948908f07d04`，已上传并在目标机匹配；目标Docker 28.3.2/linux amd64，业务和Caddy镜像均已导入并核对目标实际config ID，11个原容器元数据保持一致。配置已安装到固定独立目录，资源受限预检通过，两个角色已健康启动且重启数0，镜像ID/绑定/配额回读符合。公网HTTPS ready返回200，链信任、IP SAN与TLS1.3通过；云端账号登录/WebSocket、手机收发及自动续期尚未验收。

Caddy官方2.11.4固定镜像已通过真实Caddyfile解析、UID1000/drop ALL/仅NET_BIND_SERVICE/no-new-privileges启动、本地admin健康及容器内TCP443监听；使用无网络、无公开端口的独立环境，测试容器已清理，不代表公网TLS、签发或续期。双服务Compose实际解析通过；同时升级两镜像时业务失败导致代理记录超前的P1已修复，38项合成检查与独立原复现通过。独立Ubuntu真实age完成加密归档、业务文件恢复、当前TLS保留及损坏尾部停服前拒绝；Docker/宿主边界仍为合成，不代表真实维护容器或产品恢复。正式启用仍须实际维护、IP证书和手机跨网验证。没有自有推送项目，锁屏送达未验收；PVTC未改。

本批服务端源码6文件已提交 `de4731a77` 并推送自有快照 `da2bb7ca9`，没有推送上游。构建中只删除专用运行目标内未被服务端调用的ffmpeg系统工具，保留原业务/迁移入口及Python/curl，并为APT暂时性下载失败增加有界重试；上游目标保持不变。

2026-09-24 构建准备：原云端任务已交回companion Dockerfile的新codex-top-server target及工作区准备脚本/测试，共3文件、37项专项通过，原targets保持不变。只安装server及五个共享包，不构建手机/Web UI；仍包含该闭包的开发工具依赖，不能称已完成production裁剪。主管使用独立codextop-build环境，2CPU/4GiB/12GiB数据盘，无目录挂载，原默认Docker上下文和既有容器身份/运行状态不变。首次拉取因官方registry/auth的TLS连接失败而停止；用户随后授权修改Clash全局脚本并重拉，现已备份原脚本，仅新增三个Docker官方域名的国外流量规则，原分组和AI规则保持。运行内核规则与实际连接命中已回读，registry返回预期401、匿名auth返回200；Alpine 3.22与Node 22均已拉取成功；隔离容器实际回读x86_64，以及Node v22.23.3/架构x64。Node镜像摘要为`sha256:363e1587494626837fa7f9a23bdb453d13b0ff3c67c705c2805cfc69c2d2fad7`。首次把摘要拼入共用NODE_VERSION参数导致其他stage的-alpine引用语法无效；已恢复原参数22重试，仅修正调用，不修改源码，并核对构建实际解析摘要一致。第二次构建发现隔离build命名空间不能解析宿主代理别名，已改用该VM内核实的宿主网关地址；APT索引开始成功下载，Yarn安装已启动，均只改本次构建调用参数。业务镜像、真实SQLite迁移/开户/恢复未完成；主仓维护脚本、目标服务器与PVTC没有修改。

2026-09-24 服务器只读预检：用户同时确认没有现成Expo/Firebase项目，远程通知未配置。主管通过已有XTerminal连接查看宿主与Docker：PVTC Web容器健康、业务端口8088；80由宿主Nginx监听，443及预定18768无监听，Compose 2.38.2可用，内存available约4.6GiB、磁盘可用约74GiB；`/opt/codex-top`尚不存在。本次未安装、修改配置、启动容器或操作PVTC，安全组、公网验证路径与实际证书仍未核验。独立443的TLS-ALPN/IP证书方案继续核对官方支持。

构建网络诊断结束后，主管已停止新建的codextop-build虚拟机以释放资源，保留隔离环境待网络恢复；未停止默认Colima或既有容器。没有改动系统代理或Clash，也未采用未验证镜像站。

2026-09-24 接续核实：用户明确没有可用域名。当前隔离 APK 尚未配置匹配包名的自有 FCM/Expo 项目；HTTP 回环地址也不满足既有通知跳转的 HTTPS 校验。开发注册开关仅影响开发模式，不作为 release 的独立阻塞。保持地址校验，不用本地假通知替代远程送达。公网可评估 IP 证书，域名缺失本身不等于必须停工；仍需核对服务器 80/443 归属、安全组、验证路径及自动续期，禁止抢占 PVTC 端口。参考 [IP 证书正式可用](https://letsencrypt.org/2026/01/15/6day-and-ip-general-availability)、[Certbot IP 证书配置](https://letsencrypt.org/2026/03/11/shorter-certs-certbot)。当时尚未访问服务器；后续只读预检见上。

状态：进行中。通知隔离与路由已接线并完成专项；业务和Caddy镜像已本机构建/验证、上传校验及导入，双服务部署和公网可信HTTPS ready已通过，PVTC Web首页200、11个原容器前后状态保持。维护38项合成及独立P1复核、独立Ubuntu真实age加密与文件恢复通过；真实容器恢复后的账号/历史/文件、云端登录隔离和WebSocket、手机公网收发、自有推送/小米以及证书续期仍待验收。下方构建与下载段落保留过程，不作为当前未构建状态。用户确认目标服务器不编译。

下载进展：APT实际连接持续下载但仅约30KB/s，同一官方索引1MB有界探针中HTTP直连约50KB/s、HTTPS约27KB/s，不改APT镜像源。主管保存当前Clash脚本后仅增加Debian官方包源的精确国外分流，原有规则／分组转换结果保持；重新激活后运行规则命中，1MB完整206响应用5.95秒、约176KB/s。只结束该构建旧DIRECT连接使其重新选路，APT自动继续下载；没有重启构建、改其他分组或停止用户连接。原Node和Docker域名规则保持。

2026-09-24 容器验收准备：原云端任务交回两份仅在ignored私有目录保存的脚本，覆盖独立socket、固定镜像ID、随机隔离卷、非root就绪、stdin开户、停启持久化和现有HTTP登录身份核对。静态检查通过，尚未执行，不能替代真实Linux验收。编译修复不通过放宽TypeScript、删除集成测试或改动线上依赖绕过；PVTC保持不变。
依据：[需求](../requirement.md) v0.10、D-01～D-04、[详细设计](../detailed-design.md) v0.2 第 7～10 节。
目标：真实任务事件送达小米，通知进入正确对话；自建服务独立部署且保护 PVTC。

## 1. 依赖与交接

- M1 提供账号身份、登录代次、退出与换号结果；M4 不修改认证、开户或密码逻辑。
- M2 提供真实事件、电脑及原任务映射、有效审批请求标识；状态回声不能当新事件。
- M3 提供对话导航及通知设置展示契约；M4 独占通知响应到目标导航的逻辑，不另造页面。
- 未登录目标恢复：M4 暂存无正文目标，M1 登录结果触发校验，M3 展示正确对话或不可访问原因。
- 换号交接：M1 发出账号切换边界，M4 解除旧推送归属、清理旧通知及待处理目标，M3 清空旧展示。
- 开始产品工作前，主管确认需求、设计、任务版本和实施指令；本轮任务文档不授予实施权限。
- 设备与生产窗口由主管统一安排；未取得窗口不得并行操作手机、桌面、SSH 或生产服务。

## 2. 规划唯一文件范围

以下为实施时的规划写权，不是当前修改许可；相邻模块只读。需要扩展时先交主管协调。

| 仓库／位置 | M4 负责范围 |
| --- | --- |
| Happier `apps/ui/sources/activity/notifications/notificationRouting.ts`、`runtime/useNotificationResponseRouting.ts` | 通知解析、去重和目标跳转及对应专项 |
| Happier `apps/ui/sources/sync/domains/pending/pendingNotificationNav.ts`、`pendingNotificationAction.ts` | 服务／账号作用域、未登录目标恢复及对应专项；不重放通知审批 |
| Happier `apps/ui/sources/activity/notifications/permission/pushNotificationAccess.ts` | 推送能力检测及对应专项；权限提示界面与 M3 交接 |
| Happier `apps/cli/src/api/pushNotifications.ts`、`pushNotificationsConfig.ts`、`boundExpoPushMessage.ts` | 既有推送发送、目标约束与对应专项 |
| Happier `apps/server/sources/app/api/routes/push/pushRoutes.ts`、`app/activity/refreshAccountActivityBadgePushes.ts` | 推送设备归属与既有角标发送边界及对应专项 |
| Codex-Top `deploy/codex-top/compose.yaml`、`.env.example`、`.gitignore`、`manage.py`、`test_manage.py`、`README.md` | 独立部署配置、无秘密参数样例、发布与备份恢复入口、检查及操作说明 |
| Codex-Top 本任务文档 | M4 工作与证据；总进度和 STATUS 由主管维护 |

复用 Happier 根 `Dockerfile` 的 `server` 目标，默认不改 Dockerfile、包清单、锁文件或生成产物。
UI 路由布局、账号 owner、机器与任务 RPC、共享协议和真实凭据文件不在本文件写权内。

## 3. 执行前核实条件

- [ ] 核实 APK 的通知权限、Expo projectId、推送凭据归属及正式包配置，不复制上游账户材料。
- [ ] 核实小米实际 GMS、网络与系统后台条件；不能把 Expo／FCM 当作已可用的国内推送通道。
- [ ] 核实域名、证书、HTTPS 代理归属、长连接支持、宿主端口占用和授权运维入口。
- [ ] 只读记录 PVTC 容器健康、现有端口及 CPU／内存／磁盘余量；据此确定限额和退出条件。
- [ ] 由主管确认本轮隔离测试环境、真实设备与生产部署窗口，记录目标版本和可撤回范围。

## 4. 通知工作与验收

- [ ] 复用真实完成／待处理／异常事件，以账号、电脑、原任务和事件身份去重，不限制历史浏览范围。
- [ ] 区分首次同步、重连补齐和新事件；过期审批不继续提示仍需处理，重复投递不重复骚扰。
- [ ] 沿用 `POST /v1/push-tokens`、`GET /v1/push-tokens`、`DELETE /v1/push-tokens/:token`；不为页面另造端点。
- [ ] 校验推送 token 的设备与账号归属；退出、换号、失效 token 的清理结果可辨认，离线注销不谎称完成。
- [ ] 默认载荷只含必要定位和状态，不上传完整正文、命令或凭据；跳转前重新核验当前账号可访问性。
- [ ] 通知直达指定电脑的原会话；未登录保留受限目标，同账号登录恢复，换账号不消费旧目标。
- [ ] 冷启动、前台、后台、重复点击、切服换号与迟到回调不串线、不叠页；断网保留定位并如实提示。
- [ ] 有待处理请求时进入对话内详情；不得让旧通知按钮绕过详情或重放允许／拒绝，审批提交交 M2。
- [ ] 先验证现有 Expo 通道；不可用时提交厂商推送适配证据和选择，不自行用无限轮询替代。
- [ ] 新增或修改的方法写中文注释；目标校验、去重、退出及重试的关键步骤解释原因，部署说明写明前提、结果和失败处理。
- [ ] 使用两个合成账号、两台电脑同名任务验证隔离、去重、失效和正确跳转。
- [ ] 由主管在小米验证前台、后台、锁屏、系统回收及跨网送达；分别记录服务接收、下游 receipt、手机展示和点击定位。

## 5. 云端工作与验收

- [ ] 在隔离环境构建对应源码镜像，记录版本；构建上下文排除本地账号、数据、日志和诊断材料。
- [ ] 配置 `/opt/codex-top` 独立 Compose project、网络、数据卷、数据库、密钥和备份；不连接 PVTC 资源。
- [ ] 应用非 root 运行，容器复用 3005；宿主端口经检查后仅绑定回环，通过独立 HTTPS 入口访问。
- [ ] 配置 CPU、内存、进程、日志轮转及数据／备份保留限制；具体值依据资源基线，不预填未经核验的生产值。
- [ ] 持久化数据库、文件及服务主密钥；限制文件权限，建立一致性备份并在隔离环境实际恢复。
- [ ] 验证 HTTPS、实时长连接、跨网重连、账号隔离、通知注册和健康／就绪，不把单个 HTTP 200 当端到端通过。
- [ ] 先演练本项目升级与回退；明确数据库兼容条件，不能只退镜像却忽略数据库格式变化。
- [ ] 主管安排窗口后执行发布；前后只读对照 PVTC 健康、资源和端口，异常仅停止或撤回本项目。
- [ ] 提供安装、备份恢复、升级撤回和故障核对说明；秘密通过受控渠道交付，不进入文档或仓库。

## 6. 停止条件与交付判定

- 推送渠道未验证、真实目标无法映射、换号隔离未通过：停止宣称通知闭环完成，保留证据交主管。
- 域名／证书／入口不明、备份恢复失败、资源不足或 PVTC 异常：停止生产发布，不改用共享数据库或无限额部署。
- 需要修改其他模块 owner 或扩大生产权限时，先交接写权和范围，不并行抢写或擅自执行。
- 完成须具备账号隔离、真实送达、正确跳转、公网链路及 PVTC 前后对照证据；静态测试、构建和安装分别记录，不能互相替代。

## 7. 部署包本地审查记录

2026-09-24：原云端任务交回上述六文件，主管复跑16项检查通过。检查包含真实 Compose 静态解析、合成 SQLite 和归档；Docker 状态与 age 使用替代实现，没有访问 daemon 或远端，不证明真实加密、容器恢复或 PVTC 生产隔离已验收。

独立审查发现两项 P2：停止容器后的检查失败会绕过备份失败恢复；归档内 Compose 的 CPU／内存／进程上限未与记录配置严格比较。两项已由同一云端任务修复，主管最终24项复跑及独立原复现通过；恢复只针对重新核实过的原实例，CPU、内存/交换及PIDs严格等于记录配置。六文件写权已交回。真实 Linux 构建、age 恢复、HTTPS、自有推送、账号闭环和 PVTC 前后对照仍保留未勾选；详见[部署包审查](../../validation/codextop-deploy-package-2026-09-24.md)。
