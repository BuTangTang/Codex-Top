# M3 委派：紧凑手机界面样板

2026-09-24。用户已批准 RN／Expo 继续复用、独立 Codex Top UI、基础控件按需采用 Paper，并明确授权将部分工作交给 Cursor，指定 Grok 4.7。主管由当前 Codex 任务承担。本任务是同一 M3 的展示层样板，不是第二套正式客户端。

## 工作区与写权

- 本机现有工作区：`/Users/butang/Developer/myproject/happier`，保留全部已有未提交改动，不切分支、不 reset、不提交或 push。
- 可新增／修改：`apps/ui/sources/dev/codexTopPreview/**`；为独立预览入口所必需的 `apps/ui/app.config.js`、`apps/ui/index.ts` 以及与该门禁直接相关的既有专项测试。
- 追加品牌写权：`apps/ui/appVariantConfig.cjs` 仅统一各环境显示名为 Codex Top，相关名称预期合并到已独占配置测试；不改包标识、scheme 和环境策略。
- package.json、yarn.lock、安装依赖、构建进程、浏览器、手机和模拟器由主管独占。Paper 使用5.15.3；主管处理依赖安装，不引入其他 UI 体系。
- 不修改正式 AuthContext、MainView、SessionView、SettingsView、sync、协议、CLI、服务端、翻译词条、生产配置或 Codex 数据。不要读取私人 `.local` 账号、密钥或真实对话。需要越界时先回报。

## 设计与交互

阅读上级根/UI AGENTS 及其指定的设计、实施规则；本用户明确要求的 Codex Top 紧凑设计优先于上游品牌、复杂工作台或默认布局。参考 [首页修订方向](../homepage-revision-draft.md)和既有 U-01 图片；不要继承 Happier 可见品牌。

1. 会话：顶部小标题和搜索图标；运行中／待处理／已完成三标签，搜索按需展开；两行会话条目。首页无大筛选卡、消息预览堆叠、技术路径、刷新大卡及重复状态徽章。
2. 对话：简洁返回栏、来源小字、消息和输入框；只有示例任务存在待审批时才显示确认／拒绝。发送、审批仅更新样板的本地合成状态，不调用任何真实能力。
3. 我的：小型账号入口，少量通知／外观／连接／关于设置；没有大 Logo、二维码、终端说明、注册或找回流程。
4. 底栏展示会话／电脑／我的；电脑只提供单台明确标为示例的电脑，保持来源可辨，不自行决定多电脑聚合策略。
5. 白灰底、原品牌蓝、小图标、紧凑两行列表。触摸区域要够用，保留字体放大、键盘避让、安全区、暗色可读性。
6. 保留短标签滑动、页面转场、按压反馈和消息淡入；遵守系统减少动态效果。不要逐行入场、持续装饰动画或长弹跳。
7. D-07：只提供简体中文，不设计语言切换；界面文案、无障碍名称、空态与提示均中文，示例消息和代码允许保留原文。

## 样板隔离

使用独立 Expo Router 根目录 `sources/dev/codexTopPreview/app`。只有显式环境开关与 development 同时成立时才选择它；默认入口必须保持 `sources/app`。app.config.js 现有两个 router.root 使用同一解析结果，index.ts 预览分支避免导入正式 unistyles 初始化所关联的持久化和同步。

独立根只挂必要的 SafeArea／Gesture／Keyboard／Paper／Stack，不导入正式 Auth、Realtime、通知或 sync。所有电脑、项目、账号、消息为硬编码合成样例；页外或预览容器注明“界面预览 · 示例数据”。不要使用会写全局真实 store 的 useDemoMessages。路由样板不等于正式功能完成。

## 验证与返回

- 每个新增或修改的方法写中文用途注释，关键隔离判断和状态变化写清原因。
- 最小有效检查：开关关闭仍使用正常根，非development不能启用预览；预览导入图不接真实登录／同步／RPC。正常类型检查由主管统一执行。
- 只跑与你的文件直接有关的轻量验证，不并行 Metro、Gradle、全仓测试，不控制桌面。
- 返回精确改动文件、使用方式、已运行检查及结果、未验证事项，并明确交回写权。主管检查代码、运行样板及审核真实画面后决定正式接线。

当前状态：已在本机 Cursor 派发，任务实际标题为 **Codex Top mobile UI design**，模型为 **Grok 4.7 Extra High**。工作区显示名已改为 **Codex Top Mobile**，源路径保持不变。已确认任务书送达并开始写入隔离样板；尚未交回代码或验收。Cursor 写入范围保持上表，主管独占依赖与统一构建。
