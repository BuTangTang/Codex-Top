# Windows 验证记录

日期：2026-09-20。此文件只记录 Windows 工程实际完成的检查，不继承 macOS 仓库的测试结论。

## 0.1.10 默认空心圆环与完成信息卡

- 默认、无关注任务及历史完成状态不再显示对勾。新观察到运行/待处理任务完成时，圆环短暂显示原有完成动效，并在旁边自动显示包含任务名称的信息卡；约 5 秒后消失。已展开的监控浮窗同样显示信息，多个完成任务合并，剩余运行/待处理状态仍可见。
- 使用独立合成 SQLite/JSONL 与临时偏好运行最终 101 项针对性检查，0 失败。覆盖启动/重新选择历史完成、空状态、真实读取链的完成转换、动画进度、刷新接续、自动到期、任务重启、停止/失败、多任务合并、展开浮窗、模式切换、菜单、隐藏托盘、部分源警告及隐藏窗口后的清理。
- 信息卡原生 HWND 读回为不进入任务栏的工具窗口，弹出前后前台句柄保持一致。深浅背景与透明圆角的像素检查通过；两块屏幕靠右时卡片可完整留在工作区，与圆环保持间距。初版的系统 Right 偏移在翻转到左侧后造成 8 DIP 重叠，检查复现 2 项失败，改为明确的左右候选位置后通过。菜单用例等待实际关闭动画结束再切换模式，避免把尚未应用的界面状态误判为终态。
- 原有 Windows 窗口回归本轮重新执行 310 项，0 失败，包含透明外缘、悬停、两屏往返、展开收起、设置/选择器/菜单焦点与托盘；真实前台门槛由 Computer Use 激活独立测试窗口满足。两屏分辨率为 1920×1080 和 3200×1800，均为 120 DPI，本轮没有混合 DPI 实测。
- Computer Use 实际核对了浅色完成信息卡与到期后的空心圆环、深色空心圆环及完成信息卡，并在展开浮窗中触发合成任务完成，读回任务名称与完成状态。深色卡片的稳定画面为实色圆角，无矩形底框；完成卡出现时关闭重复的悬停工具提示。
- 30 项核心回归通过，最终 Release 构建 0 警告、0 错误，win-x64 自带运行时发布成功。打包启动首次被旧日常实例的单实例锁阻止：部署脚本统一路径格式后，按确切进程路径退出旧实例，再使用隔离设置启动最终包。新鲜窗口记录为 56×56 DIP、圆环模式、无任务栏入口；随后退出诊断实例并恢复新版日常程序。最终只有一个 0.1.10 日常实例，运行文件与发布目录哈希一致，原设置文件哈希不变。早期交互检查期间旧实例未成功退出，不能声称测试全程只有一个监控实例；最终已清理并核对。
- 累计反查范围为完成状态观察、信息卡资源/定位/生命周期、透明窗口与 DPI 刷新、菜单/拖动/模式切换、测试和版本文档。Codex 数据核心、偏好格式与 macOS 源码未改。未触发真实任务完成或写入真实任务数据；完整 FPS、所有屏幕比例与系统组合、用户对提示时长的最终验收仍未覆盖。按既有授权将源码同步到原仓库的 `windows` 分支，不发布 Release。

## 0.1.9 圆环上缘标记与完成反馈

- 用户澄清“竖杠”位于圆环上缘，不是中央数字 1。原因是待处理/出错叠加的 10 DIP 感叹号；移除该重复标记，保留原运行数量、外环颜色与呼吸。没有改数字字体或任务状态规则。
- 旧版独立检查实际读写临时合成 SQLite/日志，在 17 项检查中复现 4 项完成提示缺失；原显示只有静态文本对勾。新增所关注根任务的状态过渡观察，只在已观察到运行/等待转为完成时触发一次提示。
- 最终针对性检查 54 项、0 失败：含 10 项状态边界检查和实际 WPF/只读数据接入。覆盖启动/重选历史任务/数据源切换不重播、运行和等待后完成、多任务剩余数量与待处理保留、同时完成、提示到期、新一轮即时取消、减少动态效果、失败/停止不冒充成功，以及上缘内侧的橙色杂点像素检查。
- 对勾约 650ms 描画并轻微回弹，短暂内圈光晕，提示约 1.6 秒后消退；全体完成保持静态对勾。实际渲染的初始、中间与最终像素不同。新增刷新衔接检查曾复现进度轻微回退，现同时保留单调时间与上一帧进度下限，通过后不再回退或重新播放。
- 原有 Windows 窗口回归 310 项、0 失败，包含透明外缘、悬停、展开收起、两屏往返、菜单/设置焦点与托盘。当前两屏分别为 1920×1080 和 3200×1800，均为 120 DPI；未将早期 120/96 DPI 证据冒充本轮混合 DPI 实测。
- Computer Use 实际查看了浅色数量圆环、完成初段与静态对勾、深色完成/等待圆环，未见上缘竖杠或矩形黑边。通过实际菜单切换深色，并点击圆环展开、Escape 收回。测试全程使用独立合成任务，旧日常实例暂时退出，结束后关闭测试并恢复日常实例。
- 上述本地验证没有触发真实任务完成或修改真实任务数据。完整动画 FPS、全部显示器缩放组合及用户对动效手感的最终验收仍不在这些检查结论内。
- 累计反查发现 `MonitorStore.Error` 也承载部分历史记录不可读的警告。合成测试复现它阻止有效任务完成提示的 1 项失败；现按仍可读取的任务行观察完成状态，仅在无有效数据、暂停或加载时重建基线。最终重新执行全部 54 项针对性检查；310 项几何/交互回归沿用本轮此前结果，其代码路径未受这条警告条件修正影响。
- 最终 Release 构建 0 警告、0 错误，构建脚本中的 30 项核心回归通过。0.1.9 自带运行时包使用隔离设置实际启动，原生读回收起窗口为 56×56 DIP、圆环模式、无任务栏入口；随后退出诊断实例，不带测试参数恢复日常程序。仅一个日常实例，运行文件与最终发布目录哈希一致，原设置文件哈希前后一致，未产生新的错误日志。旧版运行文件已保留在本地忽略目录，未上传。

## 2026-09-20 独立 windows/ 目录集成

- 来源与范围见 [INTEGRATION.md](INTEGRATION.md)。导入原 Windows 仓库的 38 个受版本控制文件，新增集成说明；除 README、来源和项目/验证文档外，应用代码、资源、测试与构建脚本逐字节一致。
- 在新的 `windows/` 目录使用 .NET SDK 10.0.401 执行 `./scripts/build.ps1 -Publish`：30 项核心回归全部通过，WPF Release 构建通过（0 警告、0 错误），win-x64 自带运行时发布与便携 ZIP、源码 ZIP 生成成功。
- `dotnet build tests/CodexTop.Windows.Tests/CodexTop.Windows.Tests.csproj -c Release` 通过，0 警告、0 错误。此次仅编译窗口测试，没有重新运行交互式窗口检查或替换日常应用；下面 0.1.8 的 310 项及 0.1.7 的 14 项仍为迁移前的实际验证证据。
- 嵌套目录导出的源码包包含 39 个文件，排除了 SDK、Git 元数据、参考副本、构建输出、个人设置、认证文件、数据库和活动日志。代码及文档文本的常见令牌/个人绝对路径检查无命中；原独立仓库工作区保持干净。
- 上游 macOS 的 `Sources/`、`Tests/`、`Resources/`、`Package.swift` 和根目录 `scripts/` 无差异。当前主机为 Windows，未运行 `swift test`、`swift build -c release` 或 macOS 实机验收，不能据此声称 macOS 回归已通过。
- 仅准备源码贡献，不发布或覆盖 Release。没有新增运行时功能，也没有改变 Codex 数据或用户偏好。
- 提交前上游更新至 `d31cd9f9`（macOS build 27）。贡献分支已接上该提交，重叠的 README、文档入口和状态记录保留双方内容，并更新平台差异说明。Windows 应用代码、资源、测试及构建输入未变，因此沿用上面的实际构建与测试证据；未将上游的 macOS 改动冒充 Windows 新功能。

## 0.1.8 圆环悬停缩放

- 使用隔离临时偏好、空任务目录和不存在的 CLI，在实际 WPF 窗口运行 310 项检查，0 失败。覆盖悬停至 1.12 倍、移开恢复 1 倍、原生窗口尺寸/坐标不变、透明外角及上下左右留白、明暗外缘无杂色，以及既有展开收起、设置/菜单焦点、托盘隐藏与常驻模式。
- 实测两屏分别为 1920×1080 和 3200×1800，当前均为 120 DPI（125%）。双屏往返通过；本轮不具备不同 DPI 的硬件组合，没有把之前的 120/96 DPI 结果当成本轮重验。原生圆环窗口现为 56 DIP，其中常态可见圆环为 44 DIP，其余是用于放大的透明空间。
- 125% 下常态边缘有 205 个半透明像素，悬停后 214 个，两种主题杂色像素均为 0，圆环没有碰到客户区四边。该证据来自 WPF 像素采样与原生窗口属性检查，不能单独代表中间帧性能。
- 通过 Computer Use 实际查看浅色/深色圆环，移动鼠标悬停，点击展开、打开菜单切换深色、按 Escape 收起及拖动圆环。测试偏好读回新的坐标 (-414.4, 908.8)，仍为圆环模式。悬停期间可正常点击展开，透明空间未出现矩形底框。
- 逐块复查本次与 0.1.7 本地备份的差异：缩放动画接续、等待状态内部呼吸、拖动/菜单复位、减少动态效果分支、圆环与面板的尺寸收整、DPI 回调及版本脚本。沿用原账户、任务数据与行内取消关注证据，未重跑无关核心测试。Windows 减少动画开关、中间帧 FPS、混合 DPI 的本轮实机组合未单独验证。
- 已更新本地 0.1.8 便携包与源码包并重启日常程序；未提交、推送或远程发布。动效力度与速度仍以用户实际手感验收为准。

## 0.1.7 行内取消关注

- 使用 scripts/create-demo.py 生成的六条合成任务及独立临时偏好，CLI 指向不存在的测试文件。新建测试入口 `--fixture <合成设置文件>`，不修改日常关注列表，不接入账户。
- 14 项针对性检查通过、0 失败：合成数据加载；已完成任务即时移除；手动排除落盘；原始任务仍在数据源；已有选择器同步移除且保留其他草稿；草稿不提前提交；应用选择后两项移除均持久化；排除继续生效；自动关注不加回手动移除；可通过选择器重新加入并解除排除；移除最后一项进入空状态、无过期操作且保留选择入口。
- 实际通过 Computer Use 点击运行中和已结束任务右侧“×”：相应任务立即从面板消失，“已结束”计数同步减少，面板保持展开。独立偏好读回为关注 4 项、排除 2 项；SQLite 只读核对仍为 6 条合成任务，没有跳转任务页面。已查看浅色及深色下的按钮外观。
- 累计检查任务行事件边界、根任务标识、复用关注策略、保存通知、选择器草稿重建与关闭退订。既有数据读取、状态归约、跨屏透明绘制及外部点击收起实现未改；沿用上一版对应证据，不把它们列为本次重跑的检查。
- 已构建 0.1.7 本地便携包与源码包，保留用户日常偏好并重启运行程序。未提交、推送或远程发布。

## 0.1.6 外部点击后恢复圆环

- 检查用户偏好的显示字段：Placement=Orb，排除常驻浮窗模式。未读取或输出任务正文、账户内容。
- 在原实现上新增交互回归，复现“设置仍可见时点击外部”和“菜单打开期间跳过失焦，关闭后不补查”两项失败。此前的尺寸、像素和 Escape 检查没有覆盖这些入口。
- 修复后完整 Windows 回归 222 项检查通过、0 失败。保留真实 120/96 DPI 双屏明暗边缘回归，新增普通外部切换、快速切回、设置/选择器留在后台、隐藏设置、菜单关闭、内部焦点交接、所属对话框、托盘返回及常驻模式检查。
- 焦点测试要求真实前台：测试窗口通过 Computer Use 激活后，核对原生前台句柄及所属关系。测试开发中发现仅调用后台窗口 Activate 可改变线程内活动窗口却未获得 Windows 前台，因此增加 60 秒前台准备门槛，未满足时报告失败。菜单关闭验证等待关闭动画、延迟检查及收起动画的最终端点，最长 2 秒，不把中间帧当成失败。
- 通过 Computer Use 实际操作独立测试窗口：设置保持打开时点击外部测试窗口，面板收成小圆环；重新展开、打开右上角菜单后再点外部，菜单关闭并最终只剩圆环；打开菜单后点击面板内部，只关闭菜单且面板保持展开。操作使用合成/空数据与独立设置目录，未改变用户显示偏好。
- 累计反查主/子窗口事件、原生所属窗口判断、菜单外部捕获事件、内部菜单命令覆盖、延迟检查与退出停止、原模式分支以及版本脚本。数据核心和边缘绘制实现未改，沿用已有核心测试证据。
- 更新为 0.1.6 本地便携运行包和源码包；未提交、推送或远程发布。任意第三方不激活窗口的覆盖层、触控笔及全部 Windows 桌面特殊情形未逐项验证。

## 0.1.5 圆角菜单、设置与深色外缘

- 将 0.1.4 的不透明窗口/GDI 硬裁剪替换为 WPF 逐像素透明窗口，保留圆环和面板自身实色底、关闭矩形系统背景。此前 0.1.3–0.1.4 的“区域内全部不透明”是历史实现的测试约束，不再作为本版的绘制要求。
- 实际 Windows 集成测试 208 项检查通过、0 失败。两块物理显示器保持原 120/96 DPI，往返移动中分别验证深色与浅色、44 DIP 尺寸、原生 layered 样式、无硬区域、四角完全透明、中心不透明、展开收起原位、隐藏任务栏及最小化到托盘后恢复。
- 边缘采样仅取圆环外缘和面板弧线（避免把正文抗锯齿计为外缘）：120 DPI 圆环 205 个、面板 168 个半透明边缘像素；96 DPI 圆环 144 个、面板 136 个。两种主题下预乘颜色均符合自身底色，没有额外黑/白底色混入。该项是 WPF 渲染像素与原生窗口属性联合检查，另以实际窗口截图核对外观。
- 设置窗口在同一实例中浅→深→浅切换，偏好、背景、文字即时同步；透明圆角下拉弹层正常显示。从外部改变主题后，已有主题选择器也同步。测试使用独立临时设置与不存在的 CLI，不访问真实账户。
- 通过 Computer Use 实际查看浅色/深色圆环、深色展开面板、两种主题菜单、圆角显示方式子菜单、深浅设置及输入框；菜单的方向键导航、主题切换、设置下拉选择和滚动操作正常。最终菜单分隔线、滚动条和复选框随主题配色，没有保留原系统直角白色底块。
- 累计审查覆盖透明窗口生命周期、尺寸动画/跨屏保留逻辑、菜单命令、动态资源与事件注销、设置同步及新增控件模板。补齐复选框模板对既有 Padding 的支持，保留任务选择列表的行间距。数据核心未改，沿用已有 30 项核心测试证据。
- 本版针对当前两屏环境与明暗主题验证；物理拔插、休眠恢复、所有动画中间帧及持续性能仍未全面测试。实际截图检查不替代用户最终验收。

## 0.1.4 展开白色面板外框修复

- 在旧实现增加面板外观断言，真实 120/96 DPI 两屏往返检查中 8 项失败：系统背景仍为矩形玻璃，采样的全部面板边缘像素含半透明或暗色成分。
- 关闭系统背景、延伸窗框和非客户区绘制，改为无外描边的实色白色内容与客户区；原生圆角使用内容圆角数值，保留之前的跨屏 DPI 更新及托盘行为。
- 74 项检查通过、0 失败；包含四角裁掉、面板和圆环边缘颜色、两屏 DPI 往返、展开收起原位、最小化恢复。面板外缘在 120 DPI 采样 1632 个、96 DPI 采样 1304 个像素，暗色/半透明像素均为 0。
- 使用测试程序的 `--visual --visual-panel` 模式，通过 Computer Use 实际查看白色圆角浮窗，未见矩形灰黑底框。独立测试窗口已关闭，测试示例不进入日常数据。
- 逐块反查窗口样式、背景/主题、圆角参数、测试与版本脚本；更新为 0.1.4 本地运行包和源码包。数据核心未改，未提交、推送或远程发布。

## 0.1.3 圆环边缘像素修复

- 旧检查仅确认尺寸、DPI 和裁剪边界，没有覆盖 WPF 与原生窗口合成后的边缘颜色；用户截图暴露了这一缺口。
- 新检查在真实 120 DPI 和 96 DPI 窗口中渲染 WPF 内容，采样 GDI 圆形区域内的外缘像素。旧实现两屏及展开/收起后共 8 次采样均失败：120 DPI 首次样本中 151 个边缘像素有 127 个半透明、103 个暗像素；96 DPI 首次样本中 91 个像素有 75 个半透明、57 个暗像素。
- 圆环客户区填充同色不透明背景并移除额外外框后，全部 8 次采样的半透明像素和暗像素数均为 0，原有跨屏与最小化检查保留，总计 62 项断言通过。
- 通过独立测试程序的 `--visual` 模式让 Computer Use 发现窗口，查看实际圆环，未见点状黑色外缘；测试窗口随后已关闭。这个测试模式的任务栏可见性仅存在于测试程序中，日常程序仍不显示任务栏入口。
- 已更新 0.1.3 运行文件、使用说明和源码包。沿用核心测试证据，未改数据层，也未提交或远程发布。

## 0.1.2 混合 DPI 跨屏修复

使用 `tests/CodexTop.Windows.Tests` 在本机交互式 Windows 桌面运行独立测试窗口，向真实显示器移动，由系统发送 DPI 变化通知。测试数据和偏好均位于新建临时目录，CLI 指向不存在的文件，未接入用户任务或账户。

- 显示器 1：1920×1080、120 DPI（125%）；显示器 2：2560×1440、96 DPI（100%），工作区包含负坐标。
- 修改前先运行检查：从显示器 1 移到显示器 2，客户区已缩至 44×44 像素，但区域边界仍为约 54×54 像素；20 项断言中 2 项失败，复现用户所报边缘错位的直接几何原因。
- 第一阶段修复后，裁剪检查通过；扩展“展开/收起回原位”检查后又复现从 100% 回到 125% 的位置偏移：(960,510) 变为 (1200,638)。改为以原生像素坐标读取和移动，不使用过渡阶段可能滞后的 WPF Left/Top。
- 最终 54 项断言通过、0 失败。覆盖两屏往返、WPF 与原生 DPI 一致、44 DIP 圆环、圆形区域、面板裁剪、展开收起保持同一像素锚点、隐藏任务栏入口、最小化到托盘和恢复。
- 保留 0.1.1 的透明圆环外缘和无任务栏行为。构建与打包更新为 0.1.2；核心数据代码未改，沿用之前 30 项核心回归证据。
- 本次是实际显示器上的程序化窗口集成检查，不等于完成全部鼠标拖动手感、物理拔插、合盖或休眠验证。测试未改系统分辨率或缩放设置。
- 累计反查了本次 NativeWindow、MonitorWindow、RunningRing、测试、版本、脚本和说明改动。未提交、推送或远程发布。

## 0.1.1 圆环背景和托盘修复

- 用户截图中的圆环方形底色来自圆形内容外仍启用的系统背景。已对收起圆环关闭该背景、取消延伸窗框，并使用椭圆窗口区域；浅色圆环填充改为实色，防止失去系统背景后变灰。展开的浅色面板继续使用原背景效果。
- 实际窗口列表确认此前任务栏入口属于“监控设置 · Codex Top”。主窗口、设置、任务选择器现均设为不显示任务栏；旧 `--qa-window` 不再允许显示任务栏，仅保留尺寸诊断。
- 增加“最小化到系统托盘”；设置和任务选择器最小化时隐藏，再从原菜单打开时恢复同一个窗口和草稿。
- 实际构建通过。通过 Computer Use 检查了修正窗口的圆环、点击展开和 Escape 收起；原生诊断读回圆环为 44 × 44 DIP、复杂椭圆区域、左上角不属于窗口区域。0.1.1 发布包实际运行，读回 `ShowInTaskbar=false`，同路径仅一个实例。
- 隐藏任务栏入口后，当前 Computer Use 窗口列表不再暴露本工具窗口。新增托盘菜单和隐藏对话框恢复路径已沿代码检查，最终包中完整左右键托盘操作、浅色实色填充的最终视觉效果仍待人工确认。核心代码没有变化，沿用上一版 30 项核心测试证据，没有重复运行无关测试。
- 已逐块反查本次窗口样式、最小化与恢复、版本和打包脚本变更；没有改变真实任务数据或用户选择。仅本地更新，未提交、推送或发布远程版本。

以下为 0.1.0 的原有验证记录。

## 核心回归

已实际执行 `tests/CodexTop.Core.Tests`，30 项通过。覆盖：新轮次、旧轮次结束事件、乱序事件、同步问题按调用标识匹配、多个问题、异步问题跨完成保持、同步异步混合、失败保持、固定等待耗时、缺少开始记录、过期运行/长等待、损坏 JSON、兼容时间恢复、子孙任务聚合/跳转来源、循环/缺失父任务、首次关注、同秒新任务、手动排除、选择草稿合并、偏好重载、比例边界、半行、增量积压、文件替换、超大行、冷启动时间回查、额度来源/缺失值、任务 URL 校验。

测试数据为合成事件和独立临时目录。没有复制用户的真实任务正文作为测试样例。

## 本机实际只读接入

- Windows 11 x64，Codex CLI 0.153.0-alpha.5。
- 从当前数据目录实际读取 35 条非归档任务；其中 14 条日志位于选定目录内并可读，21 条历史记录指向选定目录以外，显示未知。没有扩大读取范围去跟随这些历史路径。
- 首次诊断共读取 917,504 字节；紧接着在记录没有变化时再次读取，增量字节为 0。
- 生产额度客户端实际完成账户读取，接口返回 1 个额度周期。未补造另一周期。诊断只输出周期数量和观察时间，没有记录账户身份或认证内容。

## 原生界面检查

通过 Computer Use 操作实际 WPF 窗口，使用隔离的合成数据库和独立偏好目录。

- 紧凑浮窗展示等待、失败、运行、结束折叠；等待计时保持不变，运行计时增长。
- 更多菜单打开，深色切换浅色玻璃，前景文字可读，后方内容模糊。
- 任务选择器搜索“任务选择器”只保留匹配项；取消勾选后选择草稿为 5 项，点击取消后持久化关注项仍为 6 项。
- 桌面圆环显示运行数和异常提示；点击展开任务，Escape 收起。
- 设置切换屏幕顶部模式，实际窗口为约 410 × 43 DIP，位于屏幕上边缘。
- 比例滑块实际拖到 120%、再到 60%；点击恢复 100% 后数字和滑块同步恢复，偏好读回一致。
- 仅托盘模式选择值已保存，仍保留同一份关注列表。托盘图标的完整左右键交互需在最终打包运行时补验。

## 独立运行包

自带运行时的 Windows x64 单文件可执行包已实际直接启动，不依赖项目内 SDK 启动。最终包读到了当前真实运行任务和当前账户返回的每周额度，窗口中没有示例数据。

打包应用中实际拖动圆环后，位置由 (1406.4,160) 更新为 (1418.4,172)，保持圆环模式；展开后拖动标题空白处，圆环锚点同步更新为约 (1398.8,192)。按 Escape 后实际窗口为 44 × 44 DIP，位置与保存的锚点一致。完整跨屏拖动仍未验。

界面自动化检查启用了 `--qa-window`，仅用于让工具发现小工具窗口并记录无任务正文的尺寸诊断；最终日常运行关闭此标志，保持系统托盘入口和原有偏好。

最终包重复启动验证通过：第二个进程在 5 秒内退出，同路径实例数保持为 1，并唤回当前面板。通过实际窗口按 Alt+F4 后进程完全退出；随后不带 `--qa-window` 重新启动日常实例，接入真实数据。发布包包含使用说明、验证记录和许可，并另附完整 Windows 工程源码包。

## 验收中发现并处理

1. 周期刷新重建按钮可能打断点击：改为比较用户可见的显示状态，只有内容变化才重建界面；时钟更新只改计时文本。
2. 同时动画化 WPF Window.Width/Height 可能使另一维度停留在旧值：改为固定临时画布并动画化内部轮廓，在完成回调中收整原生尺寸。修复后实际读回收起尺寸为 44 × 44 DIP。
3. 圆环在屏幕右侧收回时被展开画布的边界限制推向左侧：收起完成后使用圆环锚点再限制位置。
4. 设置窗口独立于监控窗口，切换到仅托盘不依赖已隐藏的监控窗口所有权。
5. 关闭路径改为可重复释放；数据源切换、暂停和退出清理文件监听及取消本工具的额度请求。

## 交付前累计审查

初始目录只有一个空文本文件；本次新增工程、核心/窗口代码、测试、构建脚本和说明。原空文件保留，参考 macOS 仓库置于 `.reference`，本地 SDK 位于 `.tools`，均排除在发布包之外。

逐项检查了数据只读打开、路径边界、日志预算/半行、任务状态来源、选择草稿与自动加入、账户请求允许范围、取消、窗口模式互斥、偏好保存和单实例入口。源码没有登录、任务创建/发送、批准、重置额度或购买调用。

代码已完成本地编写和构建。未提交、推送、部署或发布 GitHub Release。

## 边界和后续验收

- 第一版开发预览，不将本机检查表述为用户验收。
- 多显示器混合 DPI、物理拔插/合盖、休眠恢复、跨屏拖动、屏幕底部向上展开仍需实机验证。
- 动画已检查功能端点，完整中间帧、性能/FPS 与原 macOS 弹簧手感未完成对照测量。
- 点击真实任务的最终对话页面及单条问题精确定位未验；当前只尝试任务级外部链接。托盘图标完整左右键操作尚未由自动化逐项检查。
- 开机启动、代码签名、安装器和自动更新尚未实现。当前提供独立便携包。
- Windows 10 和 ARM64 未验证。实际目标为 Windows 11 x64。
- 账户未登录、不可用和缺失周期有处理；真实账号 A→B 切换和极慢网络尚未人工复现。
